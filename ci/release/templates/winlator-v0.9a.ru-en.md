# Winlator CMOD Aero.so 0.9a

## RU
- Пакет приложения: `by.aero.so.benchmark` (суффикс `.benchmark` сохранён намеренно).
- Источник `Wine/Proton` пакетов в `Contents` переведён на релизы этого репозитория.
- Улучшены каналы `stable / beta / nightly`, логирование и forensic-диагностика.
- Turnip/Adrenotools: выбор версий (latest + recent/history) и улучшенный загрузчик.

## EN
- Application package ID: `by.aero.so.benchmark` (the `.benchmark` suffix is intentional).
- `Contents` now sources `Wine/Proton` packages from this repository releases.
- Improved `stable / beta / nightly` channel handling, logging, and forensic diagnostics.
- Turnip/Adrenotools now supports version picker (latest + recent/history) and improved downloader.

## Included assets
- Winlator CMOD Aero.so APK (`by.aero.so.benchmark`)
- `SHA256SUMS`
