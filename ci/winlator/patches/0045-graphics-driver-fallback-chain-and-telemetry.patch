diff --git a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
index 1e50e59..eff114b 100644
--- a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
+++ b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
@@ -1720,27 +1720,79 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         }
 
         AdrenotoolsManager adrenotoolsManager = new AdrenotoolsManager(this);
-        DriverProbeResult driverProbe = adrenotoolsManager.probeDriver(adrenoToolsDriverId);
-        boolean useSystemVulkan = driverProbe.isUsingSystemVulkan() || !driverProbe.isUsable();
+        DriverProbeResult requestedDriverProbe = adrenotoolsManager.probeDriver(adrenoToolsDriverId);
+        DriverProbeResult effectiveDriverProbe = requestedDriverProbe;
+        String probeChain = "requested";
+        String probeReason = "requested_ok";
+        String fallbackCandidateId = "";
+        boolean requestedSystem = requestedDriverProbe.isUsingSystemVulkan();
+
+        if (!requestedSystem && !requestedDriverProbe.isUsable()) {
+            fallbackCandidateId = adrenotoolsManager.findBestFallbackDriverId(adrenoToolsDriverId);
+            if (!fallbackCandidateId.isEmpty()) {
+                DriverProbeResult fallbackProbe = adrenotoolsManager.probeDriver(fallbackCandidateId);
+                if (fallbackProbe.isUsable()) {
+                    effectiveDriverProbe = fallbackProbe;
+                    probeChain = "requested->fallback_driver";
+                    probeReason = "requested_unusable";
+                } else {
+                    probeChain = "requested->fallback_driver_failed";
+                    probeReason = "fallback_candidate_unusable";
+                }
+            } else {
+                probeChain = "requested->no_fallback_candidate";
+                probeReason = "no_fallback_candidate";
+            }
+        } else if (requestedSystem) {
+            probeChain = "requested_system";
+            probeReason = "system_selected";
+        }
 
-        Log.d("GraphicsDriverExtraction", "Driver probe -> " + driverProbe.summarize());
+        boolean useSystemVulkan = effectiveDriverProbe.isUsingSystemVulkan() || !effectiveDriverProbe.isUsable();
+        if (useSystemVulkan && !probeChain.endsWith("system")) {
+            probeChain = probeChain + "->system";
+            if ("requested_ok".equals(probeReason)) {
+                probeReason = "effective_unusable";
+            }
+        }
+
+        DriverProbeResult driverProbe = effectiveDriverProbe;
+        String driverProbeStatus = useSystemVulkan ? "fallback" : "usable";
+
+        Log.d("GraphicsDriverExtraction", "Driver probe requested -> " + requestedDriverProbe.summarize());
+        if (driverProbe != requestedDriverProbe) {
+            Log.d("GraphicsDriverExtraction", "Driver probe effective -> " + driverProbe.summarize());
+        }
         ForensicLogger.logEvent(this, useSystemVulkan ? "warn" : "info", "RUNTIME_DRIVER_PROBE",
                 forensicTraceId, "graphics_driver",
                 useSystemVulkan ? "driver probe fallback" : "driver probe usable",
                 ForensicLogger.fields(
-                        "requested_driver_id", driverProbe.getRequestedDriverId(),
+                        "requested_driver_id", requestedDriverProbe.getRequestedDriverId(),
                         "selected_driver_id", driverProbe.getSelectedDriverId(),
-                        "driver_probe_status", driverProbe.isUsable() ? "usable" : "fallback",
-                        "driver_probe_summary", driverProbe.summarize()));
+                        "driver_probe_status", driverProbeStatus,
+                        "driver_probe_chain", probeChain,
+                        "driver_probe_reason", probeReason,
+                        "fallback_candidate_id", fallbackCandidateId,
+                        "requested_probe_summary", requestedDriverProbe.summarize(),
+                        "effective_probe_summary", driverProbe.summarize()));
 
         if (driverProbe.isUsable()) {
             adrenotoolsManager.setDriverByProbe(envVars, imageFs, driverProbe);
-        } else if (!driverProbe.getRejectionReasons().isEmpty()) {
-            envVars.put("WINLATOR_DRIVER_FALLBACK_REASON", driverProbe.getRejectionReasons().toString());
+        } else {
+            String fallbackReason = !requestedDriverProbe.getRejectionReasons().isEmpty()
+                    ? requestedDriverProbe.getRejectionReasons().toString()
+                    : probeReason;
+            envVars.put("WINLATOR_DRIVER_FALLBACK_REASON", fallbackReason);
         }
 
+        envVars.put("WINLATOR_DRIVER_REQUESTED", requestedDriverProbe.getRequestedDriverId());
         envVars.put("WINLATOR_DRIVER_SELECTED", driverProbe.getSelectedDriverId());
-        envVars.put("WINLATOR_DRIVER_PROBE_STATUS", driverProbe.isUsable() ? "usable" : "fallback");
+        envVars.put("WINLATOR_DRIVER_PROBE_STATUS", driverProbeStatus);
+        envVars.put("WINLATOR_DRIVER_PROBE_CHAIN", probeChain);
+        envVars.put("WINLATOR_DRIVER_PROBE_REASON", probeReason);
+        if (!fallbackCandidateId.isEmpty()) {
+            envVars.put("WINLATOR_DRIVER_FALLBACK_CANDIDATE", fallbackCandidateId);
+        }
         if (preferences.getBoolean("enable_turnip_logs", true)) {
             File mesaLogFile = WinlatorLogUtils.createTimestampedLogFile(this, "turnip_mesa");
             envVars.put("MESA_LOG_FILE", mesaLogFile.getAbsolutePath());
@@ -1760,7 +1812,7 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         vulkanVersion = vulkanVersion + "." + vulkanVersionPatch;
         envVars.put("WRAPPER_VK_VERSION", vulkanVersion);
         envVars.put("WINLATOR_DETECTED_VK_VERSION", detectedVkVersion == null ? "" : detectedVkVersion);
-        GraphicsSuitability graphicsSuitability = GraphicsSuitability.evaluate(this, versionProbeDriverId, driverProbe);
+        GraphicsSuitability graphicsSuitability = GraphicsSuitability.evaluate(this, versionProbeDriverId, requestedDriverProbe);
         envVars.put("WINLATOR_GRAPHICS_SUITABILITY_TIER", graphicsSuitability.getTier());
         envVars.put("WINLATOR_GRAPHICS_DRIVER_FAMILY", graphicsSuitability.getDriverFamily());
         if (!graphicsSuitability.getReasons().isEmpty()) {
@@ -1775,7 +1827,8 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
                 ForensicLogger.fields(
                         "suitability_tier", graphicsSuitability.getTier(),
                         "suitability_summary", graphicsSuitability.summarize(),
-                        "driver_probe_status", driverProbe.isUsable() ? "usable" : "fallback",
+                        "driver_probe_status", driverProbeStatus,
+                        "driver_probe_chain", probeChain,
                         "driver_id", graphicsSuitability.getDriverId(),
                         "driver_family", graphicsSuitability.getDriverFamily(),
                         "detected_vk_version", graphicsSuitability.getVulkanVersion(),
@@ -1937,7 +1990,8 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
                 ForensicLogger.fields(
                         "perf_preset", perfProfile.getName(),
                         "perf_preset_source", perfProfile.getSource(),
-                        "driver_probe_status", driverProbe.isUsable() ? "usable" : "fallback",
+                        "driver_probe_status", driverProbeStatus,
+                        "driver_probe_chain", probeChain,
                         "scaleforce_enabled", envVars.get("WINLATOR_SCALEFORCE_ENABLED"),
                         "present_mode_effective", envVars.get("MESA_VK_WSI_PRESENT_MODE")));
         ForensicLogger.logEvent(this,
diff --git a/app/src/main/java/com/winlator/cmod/box64/Box64PresetManager.java b/app/src/main/java/com/winlator/cmod/box64/Box64PresetManager.java
index 7220ae6..52b18f8 100644
--- a/app/src/main/java/com/winlator/cmod/box64/Box64PresetManager.java
+++ b/app/src/main/java/com/winlator/cmod/box64/Box64PresetManager.java
@@ -374,6 +374,10 @@ public abstract class Box64PresetManager {
         spinner.setSelection(selectedPosition);
     }
 
+    public static void loadSpinner(Spinner spinner, String selectedId) {
+        loadSpinner("box64", spinner, selectedId);
+    }
+
     public static String getSpinnerSelectedId(Spinner spinner) {
         SpinnerAdapter adapter = spinner.getAdapter();
         int selectedPosition = spinner.getSelectedItemPosition();
diff --git a/app/src/main/java/com/winlator/cmod/contents/AdrenotoolsManager.java b/app/src/main/java/com/winlator/cmod/contents/AdrenotoolsManager.java
index fdefd24..1e8214b 100644
--- a/app/src/main/java/com/winlator/cmod/contents/AdrenotoolsManager.java
+++ b/app/src/main/java/com/winlator/cmod/contents/AdrenotoolsManager.java
@@ -25,6 +25,7 @@ import java.nio.file.Files;
 import java.nio.file.StandardCopyOption;
 import java.util.ArrayList;
 import java.util.HashMap;
+import java.util.Locale;
 import java.util.zip.ZipEntry;
 import java.util.zip.ZipInputStream;
 import org.json.JSONException;
@@ -301,12 +302,91 @@ public class AdrenotoolsManager {
         return result;
     }
 
+    public String findBestFallbackDriverId(String requestedDriverId) {
+        String requested = normalizeDriverId(requestedDriverId);
+        ArrayList<String> installedDrivers = enumarateInstalledDrivers();
+        if (installedDrivers.isEmpty()) return "";
+
+        String bestDriverId = "";
+        int bestScore = Integer.MIN_VALUE;
+        int[] bestVersion = new int[]{0, 0, 0};
+
+        for (String driverId : installedDrivers) {
+            if (driverId == null || driverId.trim().isEmpty()) continue;
+            if (normalizeDriverId(driverId).equals(requested)) continue;
+
+            String driverName = getDriverName(driverId);
+            String driverVersion = getDriverVersion(driverId);
+            int score = scoreFallbackCandidate(driverId, driverName, driverVersion);
+            int[] version = parseSemverLoose(driverVersion + " " + driverId + " " + driverName);
+
+            if (score > bestScore || (score == bestScore && compareVersion(version, bestVersion) > 0)) {
+                bestScore = score;
+                bestVersion = version;
+                bestDriverId = driverId;
+            }
+        }
+
+        if (bestScore < 0) return "";
+        return bestDriverId;
+    }
+
     private boolean isSystemDriverId(String adrenotoolsDriverId) {
         return adrenotoolsDriverId == null
                 || adrenotoolsDriverId.trim().isEmpty()
                 || "System".equalsIgnoreCase(adrenotoolsDriverId.trim());
     }
 
+    private static String normalizeDriverId(String value) {
+        if (value == null) return "";
+        return value.trim().toLowerCase(Locale.US);
+    }
+
+    private static int scoreFallbackCandidate(String driverId, String driverName, String driverVersion) {
+        String id = driverId != null ? driverId.toLowerCase(Locale.US) : "";
+        String name = driverName != null ? driverName.toLowerCase(Locale.US) : "";
+        String version = driverVersion != null ? driverVersion.toLowerCase(Locale.US) : "";
+        String combined = id + " " + name + " " + version;
+
+        int score = 0;
+        if (combined.contains("turnip") || combined.contains("mesa")) score += 320;
+        if (combined.contains("adreno")) score += 120;
+        if (combined.contains("vulkan")) score += 60;
+        if (combined.contains("proprietary")) score -= 20;
+        if (combined.contains("legacy")) score -= 30;
+
+        int[] parsed = parseSemverLoose(combined);
+        score += parsed[0] * 24;
+        score += parsed[1] * 6;
+        score += parsed[2];
+        return score;
+    }
+
+    private static int compareVersion(int[] a, int[] b) {
+        if (a[0] != b[0]) return a[0] - b[0];
+        if (a[1] != b[1]) return a[1] - b[1];
+        return a[2] - b[2];
+    }
+
+    private static int[] parseSemverLoose(String raw) {
+        if (raw == null || raw.isEmpty()) return new int[]{0, 0, 0};
+        int[] out = new int[]{0, 0, 0};
+        int index = 0;
+        int value = -1;
+        for (int i = 0; i < raw.length() && index < 3; i++) {
+            char c = raw.charAt(i);
+            if (c >= '0' && c <= '9') {
+                if (value < 0) value = 0;
+                value = (value * 10) + (c - '0');
+            } else if (value >= 0) {
+                out[index++] = value;
+                value = -1;
+            }
+        }
+        if (value >= 0 && index < 3) out[index] = value;
+        return out;
+    }
+
     private String readMetaField(File driverPath, String fieldName) {
         if (driverPath == null || fieldName == null || fieldName.isEmpty()) return "";
         File metaProfile = new File(driverPath, "meta.json");
diff --git a/app/src/main/java/com/winlator/cmod/graphics/GraphicsSuitability.java b/app/src/main/java/com/winlator/cmod/graphics/GraphicsSuitability.java
index bcb0699..f21ddf5 100644
--- a/app/src/main/java/com/winlator/cmod/graphics/GraphicsSuitability.java
+++ b/app/src/main/java/com/winlator/cmod/graphics/GraphicsSuitability.java
@@ -61,7 +61,7 @@ public class GraphicsSuitability {
         boolean probeFallback = probeResult != null && requestedCustom && !probeResult.isUsable();
         if (probeFallback) {
             reasons.add("driver_probe_fallback");
-            severity = Math.max(severity, 2);
+            severity = Math.max(severity, isSystemDriverId(effectiveDriverId) ? 2 : 1);
         }
 
         String normalizedRenderer = renderer != null ? renderer.trim() : "";
