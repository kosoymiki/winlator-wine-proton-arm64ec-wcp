diff --git a/app/src/main/assets/box64_env_vars.json b/app/src/main/assets/box64_env_vars.json
index 7a29671..a67cfb1 100644
--- a/app/src/main/assets/box64_env_vars.json
+++ b/app/src/main/assets/box64_env_vars.json
@@ -1,21 +1,143 @@
-[
-    {"name" : "BOX64_DYNAREC_SAFEFLAGS", "values" : ["0", "1", "2"], "defaultValue" : "1"},
-    {"name" : "BOX64_DYNAREC_FASTNAN", "values" : ["0", "1"], "toggleSwitch" : true, "defaultValue" : "1"},
-    {"name" : "BOX64_DYNAREC_FASTROUND", "values" : ["0", "1", "2"], "toggleSwitch" : false, "defaultValue" : "1"},
-    {"name" : "BOX64_DYNAREC_X87DOUBLE", "values" : ["0", "1", "2"], "toggleSwitch" : false, "defaultValue" : "0"},
-    {"name" : "BOX64_DYNAREC_BIGBLOCK", "values" : ["0", "1", "2", "3"], "defaultValue" : "2"},
-    {"name" : "BOX64_DYNAREC_STRONGMEM", "values" : ["0", "1", "2", "3"], "defaultValue" : "0"},
-    {"name" : "BOX64_DYNAREC_FORWARD", "values" : ["0", "128", "256", "512", "1024"], "defaultValue" : "512"},
-    {"name" : "BOX64_DYNAREC_CALLRET", "values" : ["0", "1"], "toggleSwitch" : true, "defaultValue" : "0"},
-    {"name" : "BOX64_DYNAREC_WAIT", "values" : ["0", "1"], "toggleSwitch" : true, "defaultValue" : "1"},
-    {"name" : "BOX64_AVX", "values" : ["0", "1", "2"], "defaultValue" : "1"},
-    {"name" : "BOX64_MAXCPU", "values" : ["0", "4", "8", "16", "32", "64"], "defaultValue" : "0"},
-    {"name" : "BOX64_UNITYPLAYER", "values" : ["0", "1"], "toggleSwitch" : true, "defaultValue" : "1"},
-    {"name" : "BOX64_DYNAREC_WEAKBARRIER", "values" : ["0", "1", "2"], "defaultValue" : "0"},
-    {"name" : "BOX64_DYNAREC_ALIGNED_ATOMICS", "values" : ["0", "1"], "toggleSwitch" : "true", "defaultValue" : "0"},
-    {"name" : "BOX64_DYNAREC_DF", "values" : ["0", "1"], "toggleSwitch" : "true", "defaultValue" : "1"},
-    {"name" : "BOX64_DYNAREC_DIRTY", "values" : ["0", "1", "2"], "toggleSwitch" : "false", "defaultValue" : "0"},
-    {"name" : "BOX64_DYNAREC_NATIVEFLAGS", "values" : ["0", "1"], "toggleSwitch" : "true", "defaultValue" : "1"},
-    {"name" : "BOX64_DYNAREC_PAUSE", "values" : ["0", "1", "2", "3"], "toggleSwitch" : "false", "defaultValue" : "0"},
-    {"name" : "BOX64_MMAP32", "values" :  ["0", "1"], "toggleSwitch" : true, "defaultValue" :  "1"}
-]
+[
+  {
+    "name": "BOX64_DYNAREC_SAFEFLAGS",
+    "group": "Dynarec",
+    "desc": ["Controls how safely dynarec preserves CPU flags (higher = safer, lower = faster)."],
+    "values": ["0", "1", "2"],
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_DYNAREC_FASTNAN",
+    "group": "Dynarec",
+    "desc": ["Use fast NaN handling. Improves speed but may change floating-point edge behavior."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_DYNAREC_FASTROUND",
+    "group": "Dynarec",
+    "desc": ["Rounding optimization level for dynarec floating-point operations."],
+    "values": ["0", "1", "2"],
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_DYNAREC_X87DOUBLE",
+    "group": "Dynarec",
+    "desc": ["Precision mode used for X87 emulation (more precise can improve compatibility)."],
+    "values": ["0", "1", "2"],
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_DYNAREC_BIGBLOCK",
+    "group": "Dynarec",
+    "desc": ["Controls dynarec block size. Larger blocks can improve throughput but may reduce stability."],
+    "values": ["0", "1", "2", "3"],
+    "defaultValue": "2"
+  },
+  {
+    "name": "BOX64_DYNAREC_STRONGMEM",
+    "group": "Dynarec",
+    "desc": ["Memory ordering strictness for dynarec. Higher values improve compatibility but cost performance."],
+    "values": ["0", "1", "2", "3"],
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_DYNAREC_FORWARD",
+    "group": "Dynarec",
+    "desc": ["Max forward jump distance considered when building dynarec blocks."],
+    "values": ["0", "128", "256", "512", "1024"],
+    "defaultValue": "512"
+  },
+  {
+    "name": "BOX64_DYNAREC_CALLRET",
+    "group": "Dynarec",
+    "desc": ["Enable CALL/RET optimization in dynarec."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_DYNAREC_WAIT",
+    "group": "Dynarec",
+    "desc": ["Make dynarec wait for background block generation when needed (compatibility vs latency)."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_AVX",
+    "group": "CPU",
+    "desc": ["Advertised AVX support level exposed to applications."],
+    "values": ["0", "1", "2"],
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_MAXCPU",
+    "group": "CPU",
+    "desc": ["Maximum CPU count reported to the guest. 0 keeps Box64 default behavior."],
+    "values": ["0", "4", "8", "16", "32", "64"],
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_UNITYPLAYER",
+    "group": "Game Hacks",
+    "desc": ["Apply UnityPlayer-specific compatibility adjustments."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_DYNAREC_WEAKBARRIER",
+    "group": "Dynarec",
+    "desc": ["Barrier optimization strength. Lower is safer, higher can be faster."],
+    "values": ["0", "1", "2"],
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_DYNAREC_ALIGNED_ATOMICS",
+    "group": "Dynarec",
+    "desc": ["Assume aligned atomics where possible for faster code generation."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_DYNAREC_DF",
+    "group": "Dynarec",
+    "desc": ["Direction Flag handling optimization in dynarec."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_DYNAREC_DIRTY",
+    "group": "Dynarec",
+    "desc": ["Controls dirty-page tracking behavior used by dynarec self-modifying code handling."],
+    "values": ["0", "1", "2"],
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_DYNAREC_NATIVEFLAGS",
+    "group": "Dynarec",
+    "desc": ["Use native host flags assumptions where possible for speed."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "1"
+  },
+  {
+    "name": "BOX64_DYNAREC_PAUSE",
+    "group": "Dynarec",
+    "desc": ["Pause instruction handling strategy in dynarec."],
+    "values": ["0", "1", "2", "3"],
+    "defaultValue": "0"
+  },
+  {
+    "name": "BOX64_MMAP32",
+    "group": "Memory",
+    "desc": ["Prefer allocations in 32-bit address space for compatibility with some titles."],
+    "values": ["0", "1"],
+    "toggleSwitch": true,
+    "defaultValue": "1"
+  }
+]
diff --git a/app/src/main/java/com/winlator/cmod/box64/Box64EditPresetDialog.java b/app/src/main/java/com/winlator/cmod/box64/Box64EditPresetDialog.java
index 45dbed8..781958f 100644
--- a/app/src/main/java/com/winlator/cmod/box64/Box64EditPresetDialog.java
+++ b/app/src/main/java/com/winlator/cmod/box64/Box64EditPresetDialog.java
@@ -85,12 +85,22 @@ public class Box64EditPresetDialog extends ContentDialog {
         LinearLayout parent = findViewById(R.id.LLContent);
         for (int i = 0; i < parent.getChildCount(); i++) {
             View child = parent.getChildAt(i);
-            String name = ((TextView)child.findViewById(R.id.TextView)).getText().toString();
+            TextView nameView = child.findViewById(R.id.TextView);
+            String name = nameView.getTag() instanceof String ? (String) nameView.getTag() : nameView.getText().toString();
 
             Spinner spinner = child.findViewById(R.id.Spinner);
             ToggleButton toggleButton = child.findViewById(R.id.ToggleButton);
+            EditText editText = child.findViewById(R.id.EditText);
             boolean toggleSwitch = toggleButton.getVisibility() == View.VISIBLE;
-            String value = toggleSwitch ? (toggleButton.isChecked() ? "1" : "0") : spinner.getSelectedItem().toString();
+            boolean hasEditText = editText.getVisibility() == View.VISIBLE;
+            String value;
+            if (hasEditText) {
+                value = editText.getText().toString();
+            } else if (toggleSwitch) {
+                value = toggleButton.isChecked() ? getToggleOnValue(child) : getToggleOffValue(child);
+            } else {
+                value = spinner.getSelectedItem().toString();
+            }
             envVars.put(name, value);
         }
         return envVars;
@@ -100,32 +110,49 @@ public class Box64EditPresetDialog extends ContentDialog {
         try {
             LinearLayout parent = findViewById(R.id.LLContent);
             LayoutInflater inflater = LayoutInflater.from(context);
-            JSONArray data = new JSONArray(FileUtils.readString(context, prefix+"_env_vars.json"));
+            JSONArray data = new JSONArray(FileUtils.readString(context, getPresetSchemaAssetPath()));
             EnvVars envVars = preset != null ? Box64PresetManager.getEnvVars(prefix, context, preset.id) : null;
 
             for (int i = 0; i < data.length(); i++) {
                 JSONObject item = data.getJSONObject(i);
                 final String name = item.getString("name");
+                final String inlineHelp = buildInlineHelp(item);
                 View child = inflater.inflate(R.layout.box64_env_var_list_item, parent, false);
-                ((TextView)child.findViewById(R.id.TextView)).setText(name);
+                TextView envVarNameView = child.findViewById(R.id.TextView);
+                envVarNameView.setText(name);
+                envVarNameView.setTag(name);
 
                 child.findViewById(R.id.BTHelp).setOnClickListener((v) -> {
                     String suffix = name.replace(prefix.toUpperCase(Locale.ENGLISH)+"_", "").toLowerCase(Locale.ENGLISH);
-                    String value = StringUtils.getString(context, "box64_env_var_help__"+suffix);
+                    String value = inlineHelp;
+                    if (value == null) value = StringUtils.getString(context, "box64_env_var_help__"+suffix);
                     if (value != null) AppUtils.showHelpBox(context, v, value);
                 });
 
                 Spinner spinner = child.findViewById(R.id.Spinner);
                 ToggleButton toggleButton = child.findViewById(R.id.ToggleButton);
+                EditText editText = child.findViewById(R.id.EditText);
                 String[] values = ArrayUtils.toStringArray(item.getJSONArray("values"));
                 String value = envVars != null && envVars.has(name) ? envVars.get(name) : item.getString("defaultValue");
 
-                if (item.optBoolean("toggleSwitch", false)) {
+                if (jsonBool(item, "toggleSwitch")) {
                     toggleButton.setVisibility(View.VISIBLE);
                     toggleButton.setEnabled(!readonly);
-                    toggleButton.setChecked(value.equals("1"));
+                    String onValue = item.optString("toggleOnValue", values.length > 1 ? values[1] : "1");
+                    String offValue = item.optString("toggleOffValue", values.length > 0 ? values[0] : "0");
+                    toggleButton.setTag(R.id.ToggleButton, onValue);
+                    toggleButton.setTag(R.id.BTHelp, offValue);
+                    toggleButton.setTextOn(item.optString("toggleOnLabel", context.getString(R.string.enabled)));
+                    toggleButton.setTextOff(item.optString("toggleOffLabel", context.getString(R.string.disabled)));
+                    toggleButton.setChecked(onValue.equals(value));
                     if (readonly) toggleButton.setAlpha(0.5f);
                 }
+                else if (jsonBool(item, "editText")) {
+                    editText.setVisibility(View.VISIBLE);
+                    editText.setEnabled(!readonly);
+                    editText.setText(value);
+                    if (readonly) editText.setAlpha(0.5f);
+                }
                 else {
                     spinner.setPopupBackgroundResource(isDarkMode ? R.drawable.content_dialog_background_dark : R.drawable.content_dialog_background);
                     spinner.setVisibility(View.VISIBLE);
@@ -140,6 +167,54 @@ public class Box64EditPresetDialog extends ContentDialog {
         catch (JSONException e) {}
     }
 
+    private String getPresetSchemaAssetPath() {
+        String direct = prefix + "_env_vars.json";
+        String fallback = "box64_env_vars.json";
+        String json = FileUtils.readString(context, direct);
+        if (json == null || json.trim().isEmpty()) return fallback;
+        return direct;
+    }
+
+    private static String buildInlineHelp(JSONObject item) {
+        JSONArray descArray = item.optJSONArray("desc");
+        if (descArray == null || descArray.length() == 0) return null;
+
+        StringBuilder sb = new StringBuilder();
+        String group = item.optString("group", "");
+        if (!group.isEmpty()) sb.append("Group: ").append(group);
+
+        for (int i = 0; i < descArray.length(); i++) {
+            String line = descArray.optString(i, "").trim();
+            if (line.isEmpty()) continue;
+            if (sb.length() > 0) sb.append('\n');
+            sb.append(line);
+        }
+        return sb.length() == 0 ? null : sb.toString();
+    }
+
+    private static boolean jsonBool(JSONObject item, String key) {
+        Object value = item.opt(key);
+        if (value instanceof Boolean) return (Boolean) value;
+        if (value instanceof String) {
+            String str = ((String) value).trim();
+            return str.equalsIgnoreCase("true") || str.equals("1") || str.equalsIgnoreCase("yes");
+        }
+        if (value instanceof Number) return ((Number) value).intValue() != 0;
+        return false;
+    }
+
+    private static String getToggleOnValue(View child) {
+        ToggleButton toggleButton = child.findViewById(R.id.ToggleButton);
+        Object value = toggleButton.getTag(R.id.ToggleButton);
+        return value instanceof String ? (String) value : "1";
+    }
+
+    private static String getToggleOffValue(View child) {
+        ToggleButton toggleButton = child.findViewById(R.id.ToggleButton);
+        Object value = toggleButton.getTag(R.id.BTHelp);
+        return value instanceof String ? (String) value : "0";
+    }
+
     private static void applyFieldSetLabelStyle(TextView textView, boolean isDarkMode) {
 //        Context context = textView.getContext();
 
diff --git a/app/src/main/java/com/winlator/cmod/fexcore/FEXCoreEditPresetDialog.java b/app/src/main/java/com/winlator/cmod/fexcore/FEXCoreEditPresetDialog.java
index aaf81d3..2415e33 100644
--- a/app/src/main/java/com/winlator/cmod/fexcore/FEXCoreEditPresetDialog.java
+++ b/app/src/main/java/com/winlator/cmod/fexcore/FEXCoreEditPresetDialog.java
@@ -96,7 +96,7 @@ public class FEXCoreEditPresetDialog extends ContentDialog {
             if (editText)
                 value = edt.getText().toString();
             else if (toggleSwitch)
-                value = toggleButton.isChecked() ? "1" : "0";
+                value = toggleButton.isChecked() ? getToggleOnValue(child) : getToggleOffValue(child);
             else
                 value = spinner.getSelectedItem().toString();
 
@@ -134,13 +134,19 @@ public class FEXCoreEditPresetDialog extends ContentDialog {
                 String[] values = ArrayUtils.toStringArray(item.getJSONArray("values"));
                 String value = envVars != null && envVars.has(name) ? envVars.get(name) : item.getString("defaultValue");
 
-                if (item.optBoolean("toggleSwitch", false)) {
+                if (jsonBool(item, "toggleSwitch")) {
                     toggleButton.setVisibility(View.VISIBLE);
                     toggleButton.setEnabled(!readonly);
-                    toggleButton.setChecked(value.equals("1"));
+                    String onValue = item.optString("toggleOnValue", values.length > 1 ? values[1] : "1");
+                    String offValue = item.optString("toggleOffValue", values.length > 0 ? values[0] : "0");
+                    toggleButton.setTag(R.id.ToggleButton, onValue);
+                    toggleButton.setTag(R.id.BTHelp, offValue);
+                    toggleButton.setTextOn(item.optString("toggleOnLabel", context.getString(R.string.enabled)));
+                    toggleButton.setTextOff(item.optString("toggleOffLabel", context.getString(R.string.disabled)));
+                    toggleButton.setChecked(onValue.equals(value));
                     if (readonly) toggleButton.setAlpha(0.5f);
                 }
-                else if (item.optBoolean("editText", false)) {
+                else if (jsonBool(item, "editText")) {
                     editText.setVisibility(View.VISIBLE);
                     editText.setEnabled(!readonly);
                     editText.setText(value);
@@ -178,6 +184,29 @@ public class FEXCoreEditPresetDialog extends ContentDialog {
         return sb.length() == 0 ? null : sb.toString();
     }
 
+    private static boolean jsonBool(JSONObject item, String key) {
+        Object value = item.opt(key);
+        if (value instanceof Boolean) return (Boolean) value;
+        if (value instanceof String) {
+            String str = ((String) value).trim();
+            return str.equalsIgnoreCase("true") || str.equals("1") || str.equalsIgnoreCase("yes");
+        }
+        if (value instanceof Number) return ((Number) value).intValue() != 0;
+        return false;
+    }
+
+    private static String getToggleOnValue(View child) {
+        ToggleButton toggleButton = child.findViewById(R.id.ToggleButton);
+        Object value = toggleButton.getTag(R.id.ToggleButton);
+        return value instanceof String ? (String) value : "1";
+    }
+
+    private static String getToggleOffValue(View child) {
+        ToggleButton toggleButton = child.findViewById(R.id.ToggleButton);
+        Object value = toggleButton.getTag(R.id.BTHelp);
+        return value instanceof String ? (String) value : "0";
+    }
+
     private static void applyFieldSetLabelStyle(TextView textView, boolean isDarkMode) {
 //        Context context = textView.getContext();
 
