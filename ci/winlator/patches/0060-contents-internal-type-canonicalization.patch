diff --git a/app/src/main/java/com/winlator/cmod/contents/ContentProfile.java b/app/src/main/java/com/winlator/cmod/contents/ContentProfile.java
index f95f321..a39489c 100644
--- a/app/src/main/java/com/winlator/cmod/contents/ContentProfile.java
+++ b/app/src/main/java/com/winlator/cmod/contents/ContentProfile.java
@@ -3,9 +3,11 @@ package com.winlator.cmod.contents;
 import androidx.annotation.NonNull;
 
 import java.util.List;
+import java.util.Locale;
 
 public class ContentProfile {
     public static final String MARK_TYPE = "type";
+    public static final String MARK_INTERNAL_TYPE = "internalType";
     public static final String MARK_VERSION_NAME = "versionName";
     public static final String MARK_VERSION_CODE = "versionCode";
     public static final String MARK_DESC = "description";
@@ -53,7 +55,7 @@ public class ContentProfile {
 
         public static ContentType getTypeByName(String name) {
             if (name == null) return null;
-            String normalized = name.trim().toLowerCase();
+            String normalized = name.trim().toLowerCase(Locale.US);
             if (normalized.isEmpty()) return null;
 
             switch (normalized) {
@@ -66,14 +68,14 @@ public class ContentProfile {
                 case "proton wine":
                 case "wine/proton":
                 case "wine-proton":
-                    normalized = CONTENT_TYPE_WINE.typeName.toLowerCase();
+                    normalized = CONTENT_TYPE_WINE.typeName.toLowerCase(Locale.US);
                     break;
                 default:
                     break;
             }
 
             for (ContentType type : ContentType.values())
-                if (type.typeName.toLowerCase().equals(normalized))
+                if (type.typeName.toLowerCase(Locale.US).equals(normalized))
                     return type;
             return null;
         }
@@ -85,6 +87,7 @@ public class ContentProfile {
     }
 
     public ContentType type;
+    public String internalType = "";
     public String verName;
     public int verCode;
     public String desc;
@@ -103,13 +106,57 @@ public class ContentProfile {
     public String artifactName = "";
     public String sha256Url = "";
 
+    public static String normalizeInternalTypeName(String name) {
+        if (name == null) return "";
+        String normalized = name.trim().toLowerCase(Locale.US);
+        if (normalized.isEmpty()) return "";
+
+        switch (normalized) {
+            case "proton-ge":
+            case "proton ge":
+                return "protonge";
+            case "proton-wine":
+            case "proton wine":
+                return "protonwine";
+            case "wine/proton":
+            case "wine-proton":
+            case "wine proton":
+                return "wine";
+            default:
+                return normalized;
+        }
+    }
+
+    public static String resolveInternalTypeName(String rawTypeName, ContentType resolvedType) {
+        String normalized = normalizeInternalTypeName(rawTypeName);
+        if (resolvedType == null) return normalized;
+
+        if (resolvedType == ContentType.CONTENT_TYPE_WINE) {
+            switch (normalized) {
+                case "proton":
+                case "protonge":
+                case "protonwine":
+                case "wine":
+                    return normalized;
+                default:
+                    return "wine";
+            }
+        }
+
+        return normalized.isEmpty() ? resolvedType.toString().toLowerCase(Locale.US) : normalized;
+    }
+
+    public String getInternalTypeName() {
+        return resolveInternalTypeName(internalType, type);
+    }
+
     public String getChannel() {
         if (channel == null || channel.trim().isEmpty()) return CHANNEL_STABLE;
-        return channel.trim().toLowerCase();
+        return channel.trim().toLowerCase(Locale.US);
     }
 
     public String getDelivery() {
-        return delivery == null ? "" : delivery.trim().toLowerCase();
+        return delivery == null ? "" : delivery.trim().toLowerCase(Locale.US);
     }
 
     public String getDisplayCategory() {
diff --git a/app/src/main/java/com/winlator/cmod/contents/ContentsManager.java b/app/src/main/java/com/winlator/cmod/contents/ContentsManager.java
index 2e9aedd..6854319 100644
--- a/app/src/main/java/com/winlator/cmod/contents/ContentsManager.java
+++ b/app/src/main/java/com/winlator/cmod/contents/ContentsManager.java
@@ -127,7 +127,9 @@ public class ContentsManager {
                     ContentProfile remoteProfile = new ContentProfile();
                     remoteProfile.remoteUrl = readRemoteUrl(object);
                     if (remoteProfile.remoteUrl == null || remoteProfile.remoteUrl.isEmpty()) continue;
-                    remoteProfile.type = ContentProfile.ContentType.getTypeByName(object.getString("type"));
+                    String rawTypeName = object.optString(ContentProfile.MARK_INTERNAL_TYPE, object.optString("type", ""));
+                    remoteProfile.type = ContentProfile.ContentType.getTypeByName(rawTypeName);
+                    remoteProfile.internalType = ContentProfile.resolveInternalTypeName(rawTypeName, remoteProfile.type);
                     remoteProfile.verName = object.optString("verName", "").trim();
                     remoteProfile.verCode = parseVerCode(object);
                     remoteProfile.desc = object.optString("description", "").trim();
@@ -311,7 +313,9 @@ public class ContentsManager {
             ContentProfile profile = new ContentProfile();
             JSONObject profileJSONObject = new JSONObject(FileUtils.readString(file));
             String typeName = profileJSONObject.getString(ContentProfile.MARK_TYPE);
+            String internalTypeName = profileJSONObject.optString(ContentProfile.MARK_INTERNAL_TYPE, typeName);
             ContentProfile.ContentType resolvedType = ContentProfile.ContentType.getTypeByName(typeName);
+            if (resolvedType == null) resolvedType = ContentProfile.ContentType.getTypeByName(internalTypeName);
             if (resolvedType == null) return null;
             String verName = profileJSONObject.getString(ContentProfile.MARK_VERSION_NAME);
             int verCode = profileJSONObject.getInt(ContentProfile.MARK_VERSION_CODE);
@@ -334,6 +338,7 @@ public class ContentsManager {
             }
 
             profile.type = resolvedType;
+            profile.internalType = ContentProfile.resolveInternalTypeName(internalTypeName, resolvedType);
             profile.verName = verName;
             profile.verCode = verCode;
             profile.desc = (desc == null || desc.trim().isEmpty()) ? verName : desc;
@@ -343,6 +348,10 @@ public class ContentsManager {
             profile.displayCategory = profileJSONObject.optString(ContentProfile.MARK_DISPLAY_CATEGORY, "");
             profile.sourceRepo = profileJSONObject.optString(ContentProfile.MARK_SOURCE_REPO, "");
             profile.releaseTag = profileJSONObject.optString(ContentProfile.MARK_RELEASE_TAG, "");
+            profile.sourceType = profileJSONObject.optString(ContentProfile.MARK_SOURCE_TYPE, "");
+            profile.sourceVersion = profileJSONObject.optString(ContentProfile.MARK_SOURCE_VERSION, "");
+            profile.artifactName = profileJSONObject.optString(ContentProfile.MARK_ARTIFACT_NAME, "");
+            profile.sha256Url = profileJSONObject.optString(ContentProfile.MARK_SHA256_URL, "");
             return profile;
         } catch (Exception e) {
             return null;
