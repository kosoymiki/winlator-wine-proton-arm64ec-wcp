diff --git a/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java b/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java
index 8b82724..f3c9399 100644
--- a/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java
+++ b/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java
@@ -239,6 +239,21 @@ public class ContainerDetailFragment extends Fragment {
 
         Spinner sUpscaleGpuLogVulkan = view.findViewById(R.id.SUpscaleGpuLogVulkan);
         sUpscaleGpuLogVulkan.setPopupBackgroundResource(isDarkMode ? R.drawable.content_dialog_background_dark : R.drawable.content_dialog_background);
+
+        Spinner sUpscaleRendererBackend = view.findViewById(R.id.SUpscaleRendererBackend);
+        sUpscaleRendererBackend.setPopupBackgroundResource(isDarkMode ? R.drawable.content_dialog_background_dark : R.drawable.content_dialog_background);
+
+        Spinner sUpscaleAsyncShaders = view.findViewById(R.id.SUpscaleAsyncShaders);
+        sUpscaleAsyncShaders.setPopupBackgroundResource(isDarkMode ? R.drawable.content_dialog_background_dark : R.drawable.content_dialog_background);
+
+        Spinner sUpscaleDiskShaderCache = view.findViewById(R.id.SUpscaleDiskShaderCache);
+        sUpscaleDiskShaderCache.setPopupBackgroundResource(isDarkMode ? R.drawable.content_dialog_background_dark : R.drawable.content_dialog_background);
+
+        Spinner sUpscaleUseSpeedLimit = view.findViewById(R.id.SUpscaleUseSpeedLimit);
+        sUpscaleUseSpeedLimit.setPopupBackgroundResource(isDarkMode ? R.drawable.content_dialog_background_dark : R.drawable.content_dialog_background);
+
+        Spinner sUpscaleForceMaxClock = view.findViewById(R.id.SUpscaleForceMaxClock);
+        sUpscaleForceMaxClock.setPopupBackgroundResource(isDarkMode ? R.drawable.content_dialog_background_dark : R.drawable.content_dialog_background);
     }
 
     private void applyDynamicStylesRecursively(View view, boolean isDarkMode) {
@@ -684,9 +699,16 @@ public class ContainerDetailFragment extends Fragment {
         Spinner sMaxAnisotropy = view.findViewById(R.id.SUpscaleMaxAnisotropy);
         Spinner sVsyncMode = view.findViewById(R.id.SUpscaleVsyncMode);
         Spinner sGpuLogVulkan = view.findViewById(R.id.SUpscaleGpuLogVulkan);
+        Spinner sRendererBackend = view.findViewById(R.id.SUpscaleRendererBackend);
+        Spinner sAsyncShaders = view.findViewById(R.id.SUpscaleAsyncShaders);
+        Spinner sDiskShaderCache = view.findViewById(R.id.SUpscaleDiskShaderCache);
+        Spinner sUseSpeedLimit = view.findViewById(R.id.SUpscaleUseSpeedLimit);
+        Spinner sForceMaxClock = view.findViewById(R.id.SUpscaleForceMaxClock);
         EditText etMultiplier = view.findViewById(R.id.ETUpscaleSwfgMultiplier);
         EditText etLatency = view.findViewById(R.id.ETUpscaleSwfgLatencyMs);
         EditText etFsrSharpness = view.findViewById(R.id.ETUpscaleFsrSharpness);
+        EditText etSpeedLimit = view.findViewById(R.id.ETUpscaleSpeedLimit);
+        EditText etTurboSpeedLimit = view.findViewById(R.id.ETUpscaleTurboSpeedLimit);
 
         UpscaleUiConfig cfg = readUpscaleConfigFromContainerOrLegacyEnv();
 
@@ -700,21 +722,32 @@ public class ContainerDetailFragment extends Fragment {
         AppUtils.setSpinnerSelectionFromValue(sMaxAnisotropy, cfg.maxAnisotropy);
         AppUtils.setSpinnerSelectionFromValue(sVsyncMode, cfg.vsyncMode);
         AppUtils.setSpinnerSelectionFromValue(sGpuLogVulkan, cfg.gpuLogVulkan);
+        AppUtils.setSpinnerSelectionFromValue(sRendererBackend, cfg.rendererBackend);
+        AppUtils.setSpinnerSelectionFromValue(sAsyncShaders, cfg.asyncShaders);
+        AppUtils.setSpinnerSelectionFromValue(sDiskShaderCache, cfg.diskShaderCache);
+        AppUtils.setSpinnerSelectionFromValue(sUseSpeedLimit, cfg.useSpeedLimit);
+        AppUtils.setSpinnerSelectionFromValue(sForceMaxClock, cfg.forceMaxClock);
 
         etMultiplier.setText(String.valueOf(clampInt(cfg.swfgMultiplier, 1, 4, 2)));
         etLatency.setText(String.valueOf(clampInt(cfg.swfgLatencyMs, 1, 50, 8)));
         etFsrSharpness.setText(String.valueOf(clampInt(cfg.fsrSharpness, 0, 100, 40)));
+        etSpeedLimit.setText(String.valueOf(clampInt(cfg.speedLimit, 25, 400, 100)));
+        etTurboSpeedLimit.setText(String.valueOf(clampInt(cfg.turboSpeedLimit, 0, 600, 0)));
 
         Runnable syncUiState = () -> {
             String policy = normalizeChoice(String.valueOf(sSwfgPolicy.getSelectedItem()), "auto");
             boolean enableSwfgDetails = !"off".equals(policy);
             String scalingFilter = normalizeUpscaleScalingFilter(String.valueOf(sScalingFilter.getSelectedItem()), "auto");
             boolean enableFsrSharpness = "fsr".equals(scalingFilter) || "scaleforce".equals(scalingFilter);
+            String speedLimitPolicy = normalizeOnOffAuto(String.valueOf(sUseSpeedLimit.getSelectedItem()), "auto");
+            boolean enableSpeedLimits = !"off".equals(speedLimitPolicy);
             sSwfgMode.setEnabled(enableSwfgDetails);
             sArtifactGuard.setEnabled(enableSwfgDetails);
             etMultiplier.setEnabled(enableSwfgDetails);
             etLatency.setEnabled(enableSwfgDetails);
             etFsrSharpness.setEnabled(enableFsrSharpness);
+            etSpeedLimit.setEnabled(enableSpeedLimits);
+            etTurboSpeedLimit.setEnabled(enableSpeedLimits);
         };
         AdapterView.OnItemSelectedListener syncListener = new AdapterView.OnItemSelectedListener() {
             @Override
@@ -729,6 +762,7 @@ public class ContainerDetailFragment extends Fragment {
         };
         sSwfgPolicy.setOnItemSelectedListener(syncListener);
         sScalingFilter.setOnItemSelectedListener(syncListener);
+        sUseSpeedLimit.setOnItemSelectedListener(syncListener);
         syncUiState.run();
     }
 
@@ -743,9 +777,16 @@ public class ContainerDetailFragment extends Fragment {
         Spinner sMaxAnisotropy = view.findViewById(R.id.SUpscaleMaxAnisotropy);
         Spinner sVsyncMode = view.findViewById(R.id.SUpscaleVsyncMode);
         Spinner sGpuLogVulkan = view.findViewById(R.id.SUpscaleGpuLogVulkan);
+        Spinner sRendererBackend = view.findViewById(R.id.SUpscaleRendererBackend);
+        Spinner sAsyncShaders = view.findViewById(R.id.SUpscaleAsyncShaders);
+        Spinner sDiskShaderCache = view.findViewById(R.id.SUpscaleDiskShaderCache);
+        Spinner sUseSpeedLimit = view.findViewById(R.id.SUpscaleUseSpeedLimit);
+        Spinner sForceMaxClock = view.findViewById(R.id.SUpscaleForceMaxClock);
         EditText etMultiplier = view.findViewById(R.id.ETUpscaleSwfgMultiplier);
         EditText etLatency = view.findViewById(R.id.ETUpscaleSwfgLatencyMs);
         EditText etFsrSharpness = view.findViewById(R.id.ETUpscaleFsrSharpness);
+        EditText etSpeedLimit = view.findViewById(R.id.ETUpscaleSpeedLimit);
+        EditText etTurboSpeedLimit = view.findViewById(R.id.ETUpscaleTurboSpeedLimit);
 
         UpscaleUiConfig cfg = new UpscaleUiConfig();
         cfg.perfPreset = normalizeChoice(String.valueOf(sPerfPreset.getSelectedItem()), "auto");
@@ -761,6 +802,13 @@ public class ContainerDetailFragment extends Fragment {
         cfg.maxAnisotropy = normalizeUpscaleMaxAnisotropy(String.valueOf(sMaxAnisotropy.getSelectedItem()), "0");
         cfg.vsyncMode = normalizeUpscaleVsyncMode(String.valueOf(sVsyncMode.getSelectedItem()), "auto");
         cfg.gpuLogVulkan = normalizeOnOffAuto(String.valueOf(sGpuLogVulkan.getSelectedItem()), "auto");
+        cfg.rendererBackend = normalizeUpscaleRendererBackend(String.valueOf(sRendererBackend.getSelectedItem()), "auto");
+        cfg.asyncShaders = normalizeOnOffAuto(String.valueOf(sAsyncShaders.getSelectedItem()), "auto");
+        cfg.diskShaderCache = normalizeOnOffAuto(String.valueOf(sDiskShaderCache.getSelectedItem()), "auto");
+        cfg.useSpeedLimit = normalizeOnOffAuto(String.valueOf(sUseSpeedLimit.getSelectedItem()), "auto");
+        cfg.speedLimit = String.valueOf(clampInt(etSpeedLimit.getText().toString(), 25, 400, 100));
+        cfg.turboSpeedLimit = String.valueOf(clampInt(etTurboSpeedLimit.getText().toString(), 0, 600, 0));
+        cfg.forceMaxClock = normalizeOnOffAuto(String.valueOf(sForceMaxClock.getSelectedItem()), "auto");
         return cfg;
     }
 
@@ -779,6 +827,13 @@ public class ContainerDetailFragment extends Fragment {
         extraData.put(Container.EXTRA_UPSCALE_MAX_ANISOTROPY, cfg.maxAnisotropy);
         extraData.put(Container.EXTRA_UPSCALE_VSYNC_MODE, cfg.vsyncMode);
         extraData.put(Container.EXTRA_UPSCALE_GPU_LOG_VULKAN, cfg.gpuLogVulkan);
+        extraData.put(Container.EXTRA_UPSCALE_RENDERER_BACKEND, cfg.rendererBackend);
+        extraData.put(Container.EXTRA_UPSCALE_ASYNC_SHADERS, cfg.asyncShaders);
+        extraData.put(Container.EXTRA_UPSCALE_DISK_SHADER_CACHE, cfg.diskShaderCache);
+        extraData.put(Container.EXTRA_UPSCALE_USE_SPEED_LIMIT, cfg.useSpeedLimit);
+        extraData.put(Container.EXTRA_UPSCALE_SPEED_LIMIT, cfg.speedLimit);
+        extraData.put(Container.EXTRA_UPSCALE_TURBO_SPEED_LIMIT, cfg.turboSpeedLimit);
+        extraData.put(Container.EXTRA_UPSCALE_FORCE_MAX_CLOCK, cfg.forceMaxClock);
         return extraData;
     }
 
@@ -798,6 +853,13 @@ public class ContainerDetailFragment extends Fragment {
             cfg.maxAnisotropy = container.getExtra(Container.EXTRA_UPSCALE_MAX_ANISOTROPY, "");
             cfg.vsyncMode = container.getExtra(Container.EXTRA_UPSCALE_VSYNC_MODE, "");
             cfg.gpuLogVulkan = container.getExtra(Container.EXTRA_UPSCALE_GPU_LOG_VULKAN, "");
+            cfg.rendererBackend = container.getExtra(Container.EXTRA_UPSCALE_RENDERER_BACKEND, "");
+            cfg.asyncShaders = container.getExtra(Container.EXTRA_UPSCALE_ASYNC_SHADERS, "");
+            cfg.diskShaderCache = container.getExtra(Container.EXTRA_UPSCALE_DISK_SHADER_CACHE, "");
+            cfg.useSpeedLimit = container.getExtra(Container.EXTRA_UPSCALE_USE_SPEED_LIMIT, "");
+            cfg.speedLimit = container.getExtra(Container.EXTRA_UPSCALE_SPEED_LIMIT, "");
+            cfg.turboSpeedLimit = container.getExtra(Container.EXTRA_UPSCALE_TURBO_SPEED_LIMIT, "");
+            cfg.forceMaxClock = container.getExtra(Container.EXTRA_UPSCALE_FORCE_MAX_CLOCK, "");
         }
 
         boolean missingStructuredUpscale = cfg.perfPreset.isEmpty()
@@ -812,7 +874,14 @@ public class ContainerDetailFragment extends Fragment {
                 && cfg.framePacingMode.isEmpty()
                 && cfg.maxAnisotropy.isEmpty()
                 && cfg.vsyncMode.isEmpty()
-                && cfg.gpuLogVulkan.isEmpty();
+                && cfg.gpuLogVulkan.isEmpty()
+                && cfg.rendererBackend.isEmpty()
+                && cfg.asyncShaders.isEmpty()
+                && cfg.diskShaderCache.isEmpty()
+                && cfg.useSpeedLimit.isEmpty()
+                && cfg.speedLimit.isEmpty()
+                && cfg.turboSpeedLimit.isEmpty()
+                && cfg.forceMaxClock.isEmpty();
 
         if (missingStructuredUpscale && isEditMode()) {
             applyLegacyUpscaleEnvFallback(cfg, new EnvVars(container.getEnvVars()));
@@ -832,6 +901,13 @@ public class ContainerDetailFragment extends Fragment {
         cfg.maxAnisotropy = normalizeUpscaleMaxAnisotropy(cfg.maxAnisotropy, "0");
         cfg.vsyncMode = normalizeUpscaleVsyncMode(cfg.vsyncMode, "auto");
         cfg.gpuLogVulkan = normalizeOnOffAuto(cfg.gpuLogVulkan, "auto");
+        cfg.rendererBackend = normalizeUpscaleRendererBackend(cfg.rendererBackend, "auto");
+        cfg.asyncShaders = normalizeOnOffAuto(cfg.asyncShaders, "auto");
+        cfg.diskShaderCache = normalizeOnOffAuto(cfg.diskShaderCache, "auto");
+        cfg.useSpeedLimit = normalizeOnOffAuto(cfg.useSpeedLimit, "auto");
+        cfg.speedLimit = String.valueOf(clampInt(cfg.speedLimit, 25, 400, 100));
+        cfg.turboSpeedLimit = String.valueOf(clampInt(cfg.turboSpeedLimit, 0, 600, 0));
+        cfg.forceMaxClock = normalizeOnOffAuto(cfg.forceMaxClock, "auto");
         return cfg;
     }
 
@@ -886,6 +962,41 @@ public class ContainerDetailFragment extends Fragment {
                 envVars.get("WINLATOR_UPSCALE_GPU_LOG_VULKAN"),
                 envVars.get("GPU_LOG_VULKAN_CALLS"));
         if (!gpuLogVulkan.isEmpty()) cfg.gpuLogVulkan = normalizeOnOffAuto(gpuLogVulkan, cfg.gpuLogVulkan);
+
+        String rendererBackend = firstNonEmpty(
+                envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"),
+                envVars.get("RENDERER_BACKEND"));
+        if (!rendererBackend.isEmpty()) cfg.rendererBackend = rendererBackend;
+
+        String asyncShaders = firstNonEmpty(
+                envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"),
+                envVars.get("RENDERER_ASYNCHRONOUS_SHADERS"));
+        if (!asyncShaders.isEmpty()) cfg.asyncShaders = normalizeOnOffAuto(asyncShaders, cfg.asyncShaders);
+
+        String diskShaderCache = firstNonEmpty(
+                envVars.get("WINLATOR_UPSCALE_DISK_SHADER_CACHE"),
+                envVars.get("RENDERER_USE_DISK_SHADER_CACHE"));
+        if (!diskShaderCache.isEmpty()) cfg.diskShaderCache = normalizeOnOffAuto(diskShaderCache, cfg.diskShaderCache);
+
+        String useSpeedLimit = firstNonEmpty(
+                envVars.get("WINLATOR_UPSCALE_USE_SPEED_LIMIT"),
+                envVars.get("RENDERER_USE_SPEED_LIMIT"));
+        if (!useSpeedLimit.isEmpty()) cfg.useSpeedLimit = normalizeOnOffAuto(useSpeedLimit, cfg.useSpeedLimit);
+
+        String speedLimit = firstNonEmpty(
+                envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT"),
+                envVars.get("RENDERER_SPEED_LIMIT"));
+        if (!speedLimit.isEmpty()) cfg.speedLimit = speedLimit;
+
+        String turboSpeedLimit = firstNonEmpty(
+                envVars.get("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT"),
+                envVars.get("RENDERER_TURBO_SPEED_LIMIT"));
+        if (!turboSpeedLimit.isEmpty()) cfg.turboSpeedLimit = turboSpeedLimit;
+
+        String forceMaxClock = firstNonEmpty(
+                envVars.get("WINLATOR_UPSCALE_FORCE_MAX_CLOCK"),
+                envVars.get("RENDERER_FORCE_MAX_CLOCK"));
+        if (!forceMaxClock.isEmpty()) cfg.forceMaxClock = normalizeOnOffAuto(forceMaxClock, cfg.forceMaxClock);
     }
 
     private static String stripUpscaleVarsFromEnv(String envString) {
@@ -911,6 +1022,13 @@ public class ContainerDetailFragment extends Fragment {
         container.putExtra(Container.EXTRA_UPSCALE_MAX_ANISOTROPY, cfg.maxAnisotropy);
         container.putExtra(Container.EXTRA_UPSCALE_VSYNC_MODE, cfg.vsyncMode);
         container.putExtra(Container.EXTRA_UPSCALE_GPU_LOG_VULKAN, cfg.gpuLogVulkan);
+        container.putExtra(Container.EXTRA_UPSCALE_RENDERER_BACKEND, cfg.rendererBackend);
+        container.putExtra(Container.EXTRA_UPSCALE_ASYNC_SHADERS, cfg.asyncShaders);
+        container.putExtra(Container.EXTRA_UPSCALE_DISK_SHADER_CACHE, cfg.diskShaderCache);
+        container.putExtra(Container.EXTRA_UPSCALE_USE_SPEED_LIMIT, cfg.useSpeedLimit);
+        container.putExtra(Container.EXTRA_UPSCALE_SPEED_LIMIT, cfg.speedLimit);
+        container.putExtra(Container.EXTRA_UPSCALE_TURBO_SPEED_LIMIT, cfg.turboSpeedLimit);
+        container.putExtra(Container.EXTRA_UPSCALE_FORCE_MAX_CLOCK, cfg.forceMaxClock);
     }
 
     private static String normalizeChoice(String value, String fallback) {
@@ -975,6 +1093,14 @@ public class ContainerDetailFragment extends Fragment {
         return fallback;
     }
 
+    private static String normalizeUpscaleRendererBackend(String value, String fallback) {
+        String normalized = normalizeChoice(value, fallback);
+        if (normalized.equals("auto") || normalized.equals("vulkan") || normalized.equals("opengl")) {
+            return normalized;
+        }
+        return fallback;
+    }
+
     private static String normalizeOnOffAuto(String value, String fallback) {
         String normalized = normalizeChoice(value, fallback);
         if (normalized.equals("auto")) return "auto";
@@ -1015,6 +1141,13 @@ public class ContainerDetailFragment extends Fragment {
         String maxAnisotropy = "0";
         String vsyncMode = "auto";
         String gpuLogVulkan = "auto";
+        String rendererBackend = "auto";
+        String asyncShaders = "auto";
+        String diskShaderCache = "auto";
+        String useSpeedLimit = "auto";
+        String speedLimit = "100";
+        String turboSpeedLimit = "0";
+        String forceMaxClock = "auto";
     }
 
     private static final String[] UPSCALE_RUNTIME_ENV_KEYS = new String[] {
@@ -1038,6 +1171,13 @@ public class ContainerDetailFragment extends Fragment {
             "WINLATOR_UPSCALE_MAX_ANISOTROPY",
             "WINLATOR_UPSCALE_VSYNC_MODE",
             "WINLATOR_UPSCALE_GPU_LOG_VULKAN",
+            "WINLATOR_UPSCALE_RENDERER_BACKEND",
+            "WINLATOR_UPSCALE_ASYNC_SHADERS",
+            "WINLATOR_UPSCALE_DISK_SHADER_CACHE",
+            "WINLATOR_UPSCALE_USE_SPEED_LIMIT",
+            "WINLATOR_UPSCALE_SPEED_LIMIT",
+            "WINLATOR_UPSCALE_TURBO_SPEED_LIMIT",
+            "WINLATOR_UPSCALE_FORCE_MAX_CLOCK",
             "WINLATOR_UPSCALE_FILTER_EFFECTIVE",
             "WINLATOR_UPSCALE_FILTER_SOURCE",
             "WINLATOR_UPSCALE_FRAME_PACING_EFFECTIVE",
@@ -1050,6 +1190,13 @@ public class ContainerDetailFragment extends Fragment {
             "MAX_ANISOTROPY",
             "RENDERER_VSYNC",
             "GPU_LOG_VULKAN_CALLS",
+            "RENDERER_BACKEND",
+            "RENDERER_ASYNCHRONOUS_SHADERS",
+            "RENDERER_USE_DISK_SHADER_CACHE",
+            "RENDERER_USE_SPEED_LIMIT",
+            "RENDERER_SPEED_LIMIT",
+            "RENDERER_TURBO_SPEED_LIMIT",
+            "RENDERER_FORCE_MAX_CLOCK",
             "DXVK_MAX_ANISOTROPY",
             "WINLATOR_GRAPHICS_SUITABILITY_TIER",
             "WINLATOR_GRAPHICS_DRIVER_FAMILY",
diff --git a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
index d82b0b1..762a189 100644
--- a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
+++ b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
@@ -1957,7 +1957,10 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
                         "renderer_resolution", envVars.get("WINLATOR_UPSCALE_RENDERER_RESOLUTION"),
                         "scaling_filter", envVars.get("WINLATOR_UPSCALE_SCALING_FILTER"),
                         "frame_pacing_mode", envVars.get("WINLATOR_UPSCALE_FRAME_PACING_MODE"),
-                        "vsync_mode", envVars.get("WINLATOR_UPSCALE_VSYNC_MODE")));
+                        "vsync_mode", envVars.get("WINLATOR_UPSCALE_VSYNC_MODE"),
+                        "renderer_backend", envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"),
+                        "async_shaders", envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"),
+                        "speed_limit", envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT")));
 
         PerfPresetResolver.apply(perfProfile, envVars, this.graphicsDriverConfig);
         applyUpscaleFilterPolicy(perfProfile);
@@ -1976,7 +1979,12 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
                         "frame_pacing", envVars.get("WINLATOR_UPSCALE_FRAME_PACING_EFFECTIVE"),
                         "vsync", envVars.get("WINLATOR_UPSCALE_VSYNC_EFFECTIVE"),
                         "max_anisotropy", envVars.get("WINLATOR_UPSCALE_MAX_ANISOTROPY"),
-                        "gpu_log_vulkan", envVars.get("WINLATOR_UPSCALE_GPU_LOG_VULKAN")));
+                        "gpu_log_vulkan", envVars.get("WINLATOR_UPSCALE_GPU_LOG_VULKAN"),
+                        "renderer_backend", envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"),
+                        "async_shaders", envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"),
+                        "disk_shader_cache", envVars.get("WINLATOR_UPSCALE_DISK_SHADER_CACHE"),
+                        "speed_limit", envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT"),
+                        "turbo_speed_limit", envVars.get("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT")));
         FrameGenerationConfig fgConfig = FrameGenerationConfig.fromEnv(envVars, perfProfile, graphicsSuitability);
         fgConfig.applyTo(envVars);
         if (fgConfig.hasOverrideConflict()) {
@@ -2049,7 +2057,12 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
                         "upscale_frame_pacing", envVars.get("WINLATOR_UPSCALE_FRAME_PACING_EFFECTIVE"),
                         "upscale_vsync", envVars.get("WINLATOR_UPSCALE_VSYNC_EFFECTIVE"),
                         "upscale_anisotropy", envVars.get("WINLATOR_UPSCALE_MAX_ANISOTROPY"),
-                        "upscale_gpu_log_vulkan", envVars.get("WINLATOR_UPSCALE_GPU_LOG_VULKAN")));
+                        "upscale_gpu_log_vulkan", envVars.get("WINLATOR_UPSCALE_GPU_LOG_VULKAN"),
+                        "upscale_renderer_backend", envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"),
+                        "upscale_async_shaders", envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"),
+                        "upscale_disk_shader_cache", envVars.get("WINLATOR_UPSCALE_DISK_SHADER_CACHE"),
+                        "upscale_speed_limit", envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT"),
+                        "upscale_turbo_speed_limit", envVars.get("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT")));
         ForensicLogger.logEvent(this,
                 "info",
                 "RUNTIME_SWFG_CONTRACT",
@@ -2150,6 +2163,27 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         String gpuLogVulkan = shortcut != null
                 ? shortcut.getExtra(Container.EXTRA_UPSCALE_GPU_LOG_VULKAN, container.getExtra(Container.EXTRA_UPSCALE_GPU_LOG_VULKAN, "auto"))
                 : container.getExtra(Container.EXTRA_UPSCALE_GPU_LOG_VULKAN, "auto");
+        String rendererBackend = shortcut != null
+                ? shortcut.getExtra(Container.EXTRA_UPSCALE_RENDERER_BACKEND, container.getExtra(Container.EXTRA_UPSCALE_RENDERER_BACKEND, "auto"))
+                : container.getExtra(Container.EXTRA_UPSCALE_RENDERER_BACKEND, "auto");
+        String asyncShaders = shortcut != null
+                ? shortcut.getExtra(Container.EXTRA_UPSCALE_ASYNC_SHADERS, container.getExtra(Container.EXTRA_UPSCALE_ASYNC_SHADERS, "auto"))
+                : container.getExtra(Container.EXTRA_UPSCALE_ASYNC_SHADERS, "auto");
+        String diskShaderCache = shortcut != null
+                ? shortcut.getExtra(Container.EXTRA_UPSCALE_DISK_SHADER_CACHE, container.getExtra(Container.EXTRA_UPSCALE_DISK_SHADER_CACHE, "auto"))
+                : container.getExtra(Container.EXTRA_UPSCALE_DISK_SHADER_CACHE, "auto");
+        String useSpeedLimit = shortcut != null
+                ? shortcut.getExtra(Container.EXTRA_UPSCALE_USE_SPEED_LIMIT, container.getExtra(Container.EXTRA_UPSCALE_USE_SPEED_LIMIT, "auto"))
+                : container.getExtra(Container.EXTRA_UPSCALE_USE_SPEED_LIMIT, "auto");
+        String speedLimit = shortcut != null
+                ? shortcut.getExtra(Container.EXTRA_UPSCALE_SPEED_LIMIT, container.getExtra(Container.EXTRA_UPSCALE_SPEED_LIMIT, "100"))
+                : container.getExtra(Container.EXTRA_UPSCALE_SPEED_LIMIT, "100");
+        String turboSpeedLimit = shortcut != null
+                ? shortcut.getExtra(Container.EXTRA_UPSCALE_TURBO_SPEED_LIMIT, container.getExtra(Container.EXTRA_UPSCALE_TURBO_SPEED_LIMIT, "0"))
+                : container.getExtra(Container.EXTRA_UPSCALE_TURBO_SPEED_LIMIT, "0");
+        String forceMaxClock = shortcut != null
+                ? shortcut.getExtra(Container.EXTRA_UPSCALE_FORCE_MAX_CLOCK, container.getExtra(Container.EXTRA_UPSCALE_FORCE_MAX_CLOCK, "auto"))
+                : container.getExtra(Container.EXTRA_UPSCALE_FORCE_MAX_CLOCK, "auto");
 
         boolean applied = false;
         if (perfPreset != null && !perfPreset.trim().isEmpty() && !"auto".equalsIgnoreCase(perfPreset)) {
@@ -2197,6 +2231,13 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         String normalizedMaxAnisotropy = normalizeUpscaleAnisotropyToken(maxAnisotropy, "0");
         String normalizedVsyncMode = normalizeUpscaleVsyncToken(vsyncMode, "auto");
         String normalizedGpuLogVulkan = normalizeUpscaleOnOffAutoToken(gpuLogVulkan, "auto");
+        String normalizedRendererBackend = normalizeUpscaleRendererBackendToken(rendererBackend, "auto");
+        String normalizedAsyncShaders = normalizeUpscaleOnOffAutoToken(asyncShaders, "auto");
+        String normalizedDiskShaderCache = normalizeUpscaleOnOffAutoToken(diskShaderCache, "auto");
+        String normalizedUseSpeedLimit = normalizeUpscaleOnOffAutoToken(useSpeedLimit, "auto");
+        String normalizedSpeedLimit = String.valueOf(clampUpscaleIntToken(speedLimit, 25, 400, 100));
+        String normalizedTurboSpeedLimit = String.valueOf(clampUpscaleIntToken(turboSpeedLimit, 0, 600, 0));
+        String normalizedForceMaxClock = normalizeUpscaleOnOffAutoToken(forceMaxClock, "auto");
 
         envVars.put("WINLATOR_UPSCALE_RENDERER_RESOLUTION", normalizedRendererResolution);
         envVars.put("WINLATOR_UPSCALE_SCALING_FILTER", normalizedScalingFilter);
@@ -2205,6 +2246,13 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         envVars.put("WINLATOR_UPSCALE_MAX_ANISOTROPY", normalizedMaxAnisotropy);
         envVars.put("WINLATOR_UPSCALE_VSYNC_MODE", normalizedVsyncMode);
         envVars.put("WINLATOR_UPSCALE_GPU_LOG_VULKAN", normalizedGpuLogVulkan);
+        envVars.put("WINLATOR_UPSCALE_RENDERER_BACKEND", normalizedRendererBackend);
+        envVars.put("WINLATOR_UPSCALE_ASYNC_SHADERS", normalizedAsyncShaders);
+        envVars.put("WINLATOR_UPSCALE_DISK_SHADER_CACHE", normalizedDiskShaderCache);
+        envVars.put("WINLATOR_UPSCALE_USE_SPEED_LIMIT", normalizedUseSpeedLimit);
+        envVars.put("WINLATOR_UPSCALE_SPEED_LIMIT", normalizedSpeedLimit);
+        envVars.put("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT", normalizedTurboSpeedLimit);
+        envVars.put("WINLATOR_UPSCALE_FORCE_MAX_CLOCK", normalizedForceMaxClock);
         envVars.put("WINLATOR_UPSCALE_SOURCE", shortcut != null ? "shortcut+container" : "container");
 
         // Eden-compatible key aliases are exported for transfer parity and downstream wrappers.
@@ -2214,6 +2262,13 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         envVars.put("FRAME_PACING_MODE", normalizedFramePacingMode);
         envVars.put("MAX_ANISOTROPY", normalizedMaxAnisotropy);
         envVars.put("RENDERER_VSYNC", normalizedVsyncMode);
+        envVars.put("RENDERER_BACKEND", normalizedRendererBackend);
+        envVars.put("RENDERER_ASYNCHRONOUS_SHADERS", toEdenBooleanToken(normalizedAsyncShaders));
+        envVars.put("RENDERER_USE_DISK_SHADER_CACHE", toEdenBooleanToken(normalizedDiskShaderCache));
+        envVars.put("RENDERER_USE_SPEED_LIMIT", toEdenBooleanToken(normalizedUseSpeedLimit));
+        envVars.put("RENDERER_SPEED_LIMIT", normalizedSpeedLimit);
+        envVars.put("RENDERER_TURBO_SPEED_LIMIT", normalizedTurboSpeedLimit);
+        envVars.put("RENDERER_FORCE_MAX_CLOCK", toEdenBooleanToken(normalizedForceMaxClock));
         envVars.put("GPU_LOG_VULKAN_CALLS",
                 "on".equals(normalizedGpuLogVulkan) ? "1" :
                         ("off".equals(normalizedGpuLogVulkan) ? "0" : "auto"));
@@ -2248,6 +2303,13 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
                             "max_anisotropy", normalizedMaxAnisotropy,
                             "vsync_mode", normalizedVsyncMode,
                             "gpu_log_vulkan_calls", normalizedGpuLogVulkan,
+                            "renderer_backend", normalizedRendererBackend,
+                            "async_shaders", normalizedAsyncShaders,
+                            "disk_shader_cache", normalizedDiskShaderCache,
+                            "use_speed_limit", normalizedUseSpeedLimit,
+                            "speed_limit", normalizedSpeedLimit,
+                            "turbo_speed_limit", normalizedTurboSpeedLimit,
+                            "force_max_clock", normalizedForceMaxClock,
                             "config_source", shortcut != null ? "shortcut+container" : "container"));
         }
     }
@@ -2364,6 +2426,20 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         return fallback;
     }
 
+    private static String normalizeUpscaleRendererBackendToken(String value, String fallback) {
+        String normalized = value != null ? value.trim().toLowerCase(Locale.US) : "";
+        if (normalized.equals("auto") || normalized.equals("vulkan") || normalized.equals("opengl")) {
+            return normalized;
+        }
+        return fallback;
+    }
+
+    private static String toEdenBooleanToken(String onOffAuto) {
+        if ("on".equals(onOffAuto)) return "1";
+        if ("off".equals(onOffAuto)) return "0";
+        return "auto";
+    }
+
     private static int clampUpscaleIntToken(String value, int min, int max, int fallback) {
         int parsed = fallback;
         try {
diff --git a/app/src/main/java/com/winlator/cmod/container/Container.java b/app/src/main/java/com/winlator/cmod/container/Container.java
index e4393af..f17e4f7 100644
--- a/app/src/main/java/com/winlator/cmod/container/Container.java
+++ b/app/src/main/java/com/winlator/cmod/container/Container.java
@@ -60,6 +60,13 @@ public class Container {
     public static final String EXTRA_UPSCALE_MAX_ANISOTROPY = "upscaleMaxAnisotropy"; // 0|2|4|8|16
     public static final String EXTRA_UPSCALE_VSYNC_MODE = "upscaleVsyncMode"; // auto|on|off
     public static final String EXTRA_UPSCALE_GPU_LOG_VULKAN = "upscaleGpuLogVulkanCalls"; // auto|on|off
+    public static final String EXTRA_UPSCALE_RENDERER_BACKEND = "upscaleRendererBackend"; // auto|vulkan|opengl
+    public static final String EXTRA_UPSCALE_ASYNC_SHADERS = "upscaleAsyncShaders"; // auto|on|off
+    public static final String EXTRA_UPSCALE_DISK_SHADER_CACHE = "upscaleDiskShaderCache"; // auto|on|off
+    public static final String EXTRA_UPSCALE_USE_SPEED_LIMIT = "upscaleUseSpeedLimit"; // auto|on|off
+    public static final String EXTRA_UPSCALE_SPEED_LIMIT = "upscaleSpeedLimit"; // 25..400
+    public static final String EXTRA_UPSCALE_TURBO_SPEED_LIMIT = "upscaleTurboSpeedLimit"; // 0..600
+    public static final String EXTRA_UPSCALE_FORCE_MAX_CLOCK = "upscaleForceMaxClock"; // auto|on|off
     public final int id;
     private String name;
     private String screenSize = DEFAULT_SCREEN_SIZE;
@@ -720,6 +727,38 @@ public class Container {
                 }
                 data.put(EXTRA_UPSCALE_GPU_LOG_VULKAN, gpuLog);
             }
+
+            if (data.has(EXTRA_UPSCALE_RENDERER_BACKEND)) {
+                String backend = normalizeLower(data.optString(EXTRA_UPSCALE_RENDERER_BACKEND, "auto"));
+                if (!(backend.equals("auto") || backend.equals("vulkan") || backend.equals("opengl"))) {
+                    backend = "auto";
+                }
+                data.put(EXTRA_UPSCALE_RENDERER_BACKEND, backend);
+            }
+
+            if (data.has(EXTRA_UPSCALE_ASYNC_SHADERS)) {
+                data.put(EXTRA_UPSCALE_ASYNC_SHADERS, normalizeOnOffAuto(data.optString(EXTRA_UPSCALE_ASYNC_SHADERS, "auto"), "auto"));
+            }
+
+            if (data.has(EXTRA_UPSCALE_DISK_SHADER_CACHE)) {
+                data.put(EXTRA_UPSCALE_DISK_SHADER_CACHE, normalizeOnOffAuto(data.optString(EXTRA_UPSCALE_DISK_SHADER_CACHE, "auto"), "auto"));
+            }
+
+            if (data.has(EXTRA_UPSCALE_USE_SPEED_LIMIT)) {
+                data.put(EXTRA_UPSCALE_USE_SPEED_LIMIT, normalizeOnOffAuto(data.optString(EXTRA_UPSCALE_USE_SPEED_LIMIT, "auto"), "auto"));
+            }
+
+            if (data.has(EXTRA_UPSCALE_SPEED_LIMIT)) {
+                data.put(EXTRA_UPSCALE_SPEED_LIMIT, clampInt(data.optString(EXTRA_UPSCALE_SPEED_LIMIT, "100"), 25, 400, 100));
+            }
+
+            if (data.has(EXTRA_UPSCALE_TURBO_SPEED_LIMIT)) {
+                data.put(EXTRA_UPSCALE_TURBO_SPEED_LIMIT, clampInt(data.optString(EXTRA_UPSCALE_TURBO_SPEED_LIMIT, "0"), 0, 600, 0));
+            }
+
+            if (data.has(EXTRA_UPSCALE_FORCE_MAX_CLOCK)) {
+                data.put(EXTRA_UPSCALE_FORCE_MAX_CLOCK, normalizeOnOffAuto(data.optString(EXTRA_UPSCALE_FORCE_MAX_CLOCK, "auto"), "auto"));
+            }
         }
         catch (JSONException ignored) {}
     }
@@ -728,6 +767,14 @@ public class Container {
         return value != null ? value.trim().toLowerCase() : "";
     }
 
+    private static String normalizeOnOffAuto(String value, String fallback) {
+        String normalized = normalizeLower(value);
+        if (normalized.equals("auto")) return "auto";
+        if (normalized.equals("on") || normalized.equals("1") || normalized.equals("true") || normalized.equals("yes")) return "on";
+        if (normalized.equals("off") || normalized.equals("0") || normalized.equals("false") || normalized.equals("no")) return "off";
+        return fallback;
+    }
+
     private static int clampInt(String value, int min, int max, int fallback) {
         int parsed = fallback;
         try {
diff --git a/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java b/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
index f8f80ce..d69c289 100644
--- a/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
+++ b/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
@@ -547,7 +547,10 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
                             "upscale_fsr_sharpness", envVars.get("WINLATOR_UPSCALE_FSR_SHARPNESS"),
                             "upscale_frame_pacing", envVars.get("WINLATOR_UPSCALE_FRAME_PACING_EFFECTIVE"),
                             "upscale_vsync", envVars.get("WINLATOR_UPSCALE_VSYNC_EFFECTIVE"),
-                            "upscale_anisotropy", envVars.get("WINLATOR_UPSCALE_MAX_ANISOTROPY")));
+                            "upscale_anisotropy", envVars.get("WINLATOR_UPSCALE_MAX_ANISOTROPY"),
+                            "upscale_renderer_backend", envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"),
+                            "upscale_async_shaders", envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"),
+                            "upscale_speed_limit", envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT")));
         }
 
         // Construct the command without Box64 to the Wine executable
@@ -662,6 +665,13 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
                             "upscale_vsync", envVars.get("WINLATOR_UPSCALE_VSYNC_EFFECTIVE"),
                             "upscale_anisotropy", envVars.get("WINLATOR_UPSCALE_MAX_ANISOTROPY"),
                             "upscale_gpu_log_vulkan", envVars.get("WINLATOR_UPSCALE_GPU_LOG_VULKAN"),
+                            "upscale_renderer_backend", envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"),
+                            "upscale_async_shaders", envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"),
+                            "upscale_disk_shader_cache", envVars.get("WINLATOR_UPSCALE_DISK_SHADER_CACHE"),
+                            "upscale_use_speed_limit", envVars.get("WINLATOR_UPSCALE_USE_SPEED_LIMIT"),
+                            "upscale_speed_limit", envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT"),
+                            "upscale_turbo_speed_limit", envVars.get("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT"),
+                            "upscale_force_max_clock", envVars.get("WINLATOR_UPSCALE_FORCE_MAX_CLOCK"),
                             "command", command,
                             "env_hash", ForensicLogger.hashEnvVars(envVars)));
         }
@@ -863,6 +873,80 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
             changed = true;
         }
 
+        String rendererBackend = normalizeRendererBackendToken(envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"), envVars.get("RENDERER_BACKEND"), "auto");
+        if (!rendererBackend.equals(envVars.get("WINLATOR_UPSCALE_RENDERER_BACKEND"))) {
+            envVars.put("WINLATOR_UPSCALE_RENDERER_BACKEND", rendererBackend);
+            changed = true;
+        }
+        if (!rendererBackend.equals(envVars.get("RENDERER_BACKEND"))) {
+            envVars.put("RENDERER_BACKEND", rendererBackend);
+            changed = true;
+        }
+
+        String asyncShaders = normalizeOnOffAutoToken(firstNonEmpty(envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"), envVars.get("RENDERER_ASYNCHRONOUS_SHADERS")), "auto");
+        if (!asyncShaders.equals(envVars.get("WINLATOR_UPSCALE_ASYNC_SHADERS"))) {
+            envVars.put("WINLATOR_UPSCALE_ASYNC_SHADERS", asyncShaders);
+            changed = true;
+        }
+        String asyncNumeric = onOffAutoToNumeric(asyncShaders);
+        if (!asyncNumeric.equals(envVars.get("RENDERER_ASYNCHRONOUS_SHADERS"))) {
+            envVars.put("RENDERER_ASYNCHRONOUS_SHADERS", asyncNumeric);
+            changed = true;
+        }
+
+        String diskShaderCache = normalizeOnOffAutoToken(firstNonEmpty(envVars.get("WINLATOR_UPSCALE_DISK_SHADER_CACHE"), envVars.get("RENDERER_USE_DISK_SHADER_CACHE")), "auto");
+        if (!diskShaderCache.equals(envVars.get("WINLATOR_UPSCALE_DISK_SHADER_CACHE"))) {
+            envVars.put("WINLATOR_UPSCALE_DISK_SHADER_CACHE", diskShaderCache);
+            changed = true;
+        }
+        String diskShaderCacheNumeric = onOffAutoToNumeric(diskShaderCache);
+        if (!diskShaderCacheNumeric.equals(envVars.get("RENDERER_USE_DISK_SHADER_CACHE"))) {
+            envVars.put("RENDERER_USE_DISK_SHADER_CACHE", diskShaderCacheNumeric);
+            changed = true;
+        }
+
+        String useSpeedLimit = normalizeOnOffAutoToken(firstNonEmpty(envVars.get("WINLATOR_UPSCALE_USE_SPEED_LIMIT"), envVars.get("RENDERER_USE_SPEED_LIMIT")), "auto");
+        if (!useSpeedLimit.equals(envVars.get("WINLATOR_UPSCALE_USE_SPEED_LIMIT"))) {
+            envVars.put("WINLATOR_UPSCALE_USE_SPEED_LIMIT", useSpeedLimit);
+            changed = true;
+        }
+        String useSpeedLimitNumeric = onOffAutoToNumeric(useSpeedLimit);
+        if (!useSpeedLimitNumeric.equals(envVars.get("RENDERER_USE_SPEED_LIMIT"))) {
+            envVars.put("RENDERER_USE_SPEED_LIMIT", useSpeedLimitNumeric);
+            changed = true;
+        }
+
+        String speedLimit = String.valueOf(clampInt(firstNonEmpty(envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT"), envVars.get("RENDERER_SPEED_LIMIT")), 25, 400, 100));
+        if (!speedLimit.equals(envVars.get("WINLATOR_UPSCALE_SPEED_LIMIT"))) {
+            envVars.put("WINLATOR_UPSCALE_SPEED_LIMIT", speedLimit);
+            changed = true;
+        }
+        if (!speedLimit.equals(envVars.get("RENDERER_SPEED_LIMIT"))) {
+            envVars.put("RENDERER_SPEED_LIMIT", speedLimit);
+            changed = true;
+        }
+
+        String turboSpeedLimit = String.valueOf(clampInt(firstNonEmpty(envVars.get("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT"), envVars.get("RENDERER_TURBO_SPEED_LIMIT")), 0, 600, 0));
+        if (!turboSpeedLimit.equals(envVars.get("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT"))) {
+            envVars.put("WINLATOR_UPSCALE_TURBO_SPEED_LIMIT", turboSpeedLimit);
+            changed = true;
+        }
+        if (!turboSpeedLimit.equals(envVars.get("RENDERER_TURBO_SPEED_LIMIT"))) {
+            envVars.put("RENDERER_TURBO_SPEED_LIMIT", turboSpeedLimit);
+            changed = true;
+        }
+
+        String forceMaxClock = normalizeOnOffAutoToken(firstNonEmpty(envVars.get("WINLATOR_UPSCALE_FORCE_MAX_CLOCK"), envVars.get("RENDERER_FORCE_MAX_CLOCK")), "auto");
+        if (!forceMaxClock.equals(envVars.get("WINLATOR_UPSCALE_FORCE_MAX_CLOCK"))) {
+            envVars.put("WINLATOR_UPSCALE_FORCE_MAX_CLOCK", forceMaxClock);
+            changed = true;
+        }
+        String forceMaxClockNumeric = onOffAutoToNumeric(forceMaxClock);
+        if (!forceMaxClockNumeric.equals(envVars.get("RENDERER_FORCE_MAX_CLOCK"))) {
+            envVars.put("RENDERER_FORCE_MAX_CLOCK", forceMaxClockNumeric);
+            changed = true;
+        }
+
         return changed;
     }
 
@@ -923,6 +1007,27 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
         return defaultValue;
     }
 
+    private String normalizeRendererBackendToken(String primary, String fallback, String defaultValue) {
+        String source = !isEmpty(primary) ? primary : fallback;
+        String lowered = source != null ? source.trim().toLowerCase() : "";
+        if (lowered.equals("auto") || lowered.equals("vulkan") || lowered.equals("opengl")) {
+            return lowered;
+        }
+        return defaultValue;
+    }
+
+    private String onOffAutoToNumeric(String value) {
+        if ("on".equals(value)) return "1";
+        if ("off".equals(value)) return "0";
+        return "auto";
+    }
+
+    private String firstNonEmpty(String a, String b) {
+        if (!isEmpty(a)) return a;
+        if (!isEmpty(b)) return b;
+        return "";
+    }
+
     private String normalizeBoolToken(String value) {
         String lowered = value != null ? value.trim().toLowerCase() : "";
         return ("1".equals(lowered) || "true".equals(lowered) || "yes".equals(lowered) || "on".equals(lowered)) ? "1" : "0";
diff --git a/app/src/main/res/layout/container_detail_fragment.xml b/app/src/main/res/layout/container_detail_fragment.xml
index c0a06b5..f638765 100644
--- a/app/src/main/res/layout/container_detail_fragment.xml
+++ b/app/src/main/res/layout/container_detail_fragment.xml
@@ -822,6 +822,111 @@
                             android:id="@+id/SUpscaleGpuLogVulkan"
                             android:layout_width="match_parent"
                             android:entries="@array/upscale_gpu_log_vulkan_entries" />
+
+                        <TextView
+                            android:layout_width="wrap_content"
+                            android:layout_height="wrap_content"
+                            android:text="@string/upscale_renderer_backend" />
+
+                        <Spinner
+                            style="@style/ComboBox"
+                            android:id="@+id/SUpscaleRendererBackend"
+                            android:layout_width="match_parent"
+                            android:entries="@array/upscale_renderer_backend_entries" />
+
+                        <TextView
+                            android:layout_width="wrap_content"
+                            android:layout_height="wrap_content"
+                            android:text="@string/upscale_async_shaders" />
+
+                        <Spinner
+                            style="@style/ComboBox"
+                            android:id="@+id/SUpscaleAsyncShaders"
+                            android:layout_width="match_parent"
+                            android:entries="@array/upscale_on_off_auto_entries" />
+
+                        <TextView
+                            android:layout_width="wrap_content"
+                            android:layout_height="wrap_content"
+                            android:text="@string/upscale_disk_shader_cache" />
+
+                        <Spinner
+                            style="@style/ComboBox"
+                            android:id="@+id/SUpscaleDiskShaderCache"
+                            android:layout_width="match_parent"
+                            android:entries="@array/upscale_on_off_auto_entries" />
+
+                        <TextView
+                            android:layout_width="wrap_content"
+                            android:layout_height="wrap_content"
+                            android:text="@string/upscale_use_speed_limit" />
+
+                        <Spinner
+                            style="@style/ComboBox"
+                            android:id="@+id/SUpscaleUseSpeedLimit"
+                            android:layout_width="match_parent"
+                            android:entries="@array/upscale_use_speed_limit_entries" />
+
+                        <LinearLayout
+                            android:layout_width="match_parent"
+                            android:layout_height="wrap_content"
+                            android:orientation="horizontal">
+
+                            <LinearLayout
+                                android:layout_width="0dp"
+                                android:layout_height="wrap_content"
+                                android:layout_weight="1"
+                                android:orientation="vertical">
+
+                                <TextView
+                                    android:layout_width="wrap_content"
+                                    android:layout_height="wrap_content"
+                                    android:text="@string/upscale_speed_limit" />
+
+                                <EditText
+                                    style="@style/AppTheme"
+                                    android:id="@+id/ETUpscaleSpeedLimit"
+                                    android:layout_width="match_parent"
+                                    android:layout_height="wrap_content"
+                                    android:inputType="number"
+                                    android:maxLength="3" />
+                            </LinearLayout>
+
+                            <Space
+                                android:layout_width="8dp"
+                                android:layout_height="1dp" />
+
+                            <LinearLayout
+                                android:layout_width="0dp"
+                                android:layout_height="wrap_content"
+                                android:layout_weight="1"
+                                android:orientation="vertical">
+
+                                <TextView
+                                    android:layout_width="wrap_content"
+                                    android:layout_height="wrap_content"
+                                    android:text="@string/upscale_turbo_speed_limit" />
+
+                                <EditText
+                                    style="@style/AppTheme"
+                                    android:id="@+id/ETUpscaleTurboSpeedLimit"
+                                    android:layout_width="match_parent"
+                                    android:layout_height="wrap_content"
+                                    android:inputType="number"
+                                    android:maxLength="3" />
+                            </LinearLayout>
+                        </LinearLayout>
+
+                        <TextView
+                            android:layout_width="wrap_content"
+                            android:layout_height="wrap_content"
+                            android:text="@string/upscale_force_max_clock" />
+
+                        <Spinner
+                            style="@style/ComboBox"
+                            android:id="@+id/SUpscaleForceMaxClock"
+                            android:layout_width="match_parent"
+                            android:entries="@array/upscale_on_off_auto_entries" />
                     </LinearLayout>
 
                     <TextView
diff --git a/app/src/main/res/values/arrays.xml b/app/src/main/res/values/arrays.xml
index fed7940..4b1262c 100644
--- a/app/src/main/res/values/arrays.xml
+++ b/app/src/main/res/values/arrays.xml
@@ -212,6 +212,21 @@
         <item>off</item>
         <item>on</item>
     </string-array>
+    <string-array name="upscale_renderer_backend_entries">
+        <item>auto</item>
+        <item>vulkan</item>
+        <item>opengl</item>
+    </string-array>
+    <string-array name="upscale_on_off_auto_entries">
+        <item>auto</item>
+        <item>off</item>
+        <item>on</item>
+    </string-array>
+    <string-array name="upscale_use_speed_limit_entries">
+        <item>auto</item>
+        <item>off</item>
+        <item>on</item>
+    </string-array>
     <string-array name="vkd3d_version_entries">
         <item>None</item>
         <item>2.8</item>
diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
index d8477b2..7ca1d20 100644
--- a/app/src/main/res/values/strings.xml
+++ b/app/src/main/res/values/strings.xml
@@ -153,6 +153,13 @@
     <string name="upscale_max_anisotropy">Max anisotropy</string>
     <string name="upscale_vsync_mode">VSync mode</string>
     <string name="upscale_gpu_log_vulkan">Vulkan API logging</string>
+    <string name="upscale_renderer_backend">Renderer backend</string>
+    <string name="upscale_async_shaders">Asynchronous shaders</string>
+    <string name="upscale_disk_shader_cache">Disk shader cache</string>
+    <string name="upscale_use_speed_limit">Use speed limit</string>
+    <string name="upscale_speed_limit">Speed limit (%)</string>
+    <string name="upscale_turbo_speed_limit">Turbo speed limit (%)</string>
+    <string name="upscale_force_max_clock">Force max GPU clock</string>
     <string name="box64">Box64</string>
     <string name="system">System</string>
     <string name="box64_preset">Box64 Preset</string>
