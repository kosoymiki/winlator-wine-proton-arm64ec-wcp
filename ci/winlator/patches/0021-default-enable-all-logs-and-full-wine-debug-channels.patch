diff --git a/app/src/main/java/com/winlator/cmod/SettingsFragment.java b/app/src/main/java/com/winlator/cmod/SettingsFragment.java
index 5b8240f..b95364a 100644
--- a/app/src/main/java/com/winlator/cmod/SettingsFragment.java
+++ b/app/src/main/java/com/winlator/cmod/SettingsFragment.java
@@ -75,8 +75,9 @@ import java.util.List;
 import java.util.concurrent.ExecutorService;
 import java.util.concurrent.Executors;
 
-public class SettingsFragment extends Fragment {
-    public static final String DEFAULT_WINE_DEBUG_CHANNELS = "warn,err,fixme";
+public class SettingsFragment extends Fragment {
+    public static final String DEFAULT_WINE_DEBUG_CHANNELS = "warn,err,fixme";
+    private static volatile String cachedDefaultWineDebugChannels;
     public static final String DEFAULT_WINLATOR_PATH = Environment.getExternalStorageDirectory().getPath() + "/Winlator";
     public static final String DEFAULT_SHORTCUT_EXPORT_PATH = DEFAULT_WINLATOR_PATH + "/Shortcuts";
     private Callback<Uri> installSoundFontCallback;
@@ -264,19 +265,20 @@ public class SettingsFragment extends Fragment {
         }
 
         final CheckBox cbEnableWineDebug = view.findViewById(R.id.CBEnableWineDebug);
-        cbEnableWineDebug.setChecked(preferences.getBoolean("enable_wine_debug", false));
-
-        final ArrayList<String> wineDebugChannels = new ArrayList<>(Arrays.asList(preferences.getString("wine_debug_channels", DEFAULT_WINE_DEBUG_CHANNELS).split(",")));
-        loadWineDebugChannels(view, wineDebugChannels);
-
+        cbEnableWineDebug.setChecked(preferences.getBoolean("enable_wine_debug", true));
+
+        final ArrayList<String> wineDebugChannels = new ArrayList<>(Arrays.asList(
+                preferences.getString("wine_debug_channels", getDefaultWineDebugChannels(context)).split(",")));
+        loadWineDebugChannels(view, wineDebugChannels);
+
         final CheckBox cbEnableBox64Logs = view.findViewById(R.id.CBEnableBox64Logs);
-        cbEnableBox64Logs.setChecked(preferences.getBoolean("enable_box64_logs", false));
+        cbEnableBox64Logs.setChecked(preferences.getBoolean("enable_box64_logs", true));
         final CheckBox cbEnableFEXLogs = view.findViewById(R.id.CBEnableFEXLogs);
-        cbEnableFEXLogs.setChecked(preferences.getBoolean("enable_fex_logs", false));
+        cbEnableFEXLogs.setChecked(preferences.getBoolean("enable_fex_logs", true));
         final CheckBox cbEnableVulkanLogs = view.findViewById(R.id.CBEnableVulkanLogs);
-        cbEnableVulkanLogs.setChecked(preferences.getBoolean("enable_vulkan_api_dump", false));
+        cbEnableVulkanLogs.setChecked(preferences.getBoolean("enable_vulkan_api_dump", true));
         final CheckBox cbEnableTurnipLogs = view.findViewById(R.id.CBEnableTurnipLogs);
-        cbEnableTurnipLogs.setChecked(preferences.getBoolean("enable_turnip_logs", false));
+        cbEnableTurnipLogs.setChecked(preferences.getBoolean("enable_turnip_logs", true));
 
         final TextView tvCursorSpeed = view.findViewById(R.id.TVCursorSpeed);
         final SeekBar sbCursorSpeed = view.findViewById(R.id.SBCursorSpeed);
@@ -647,9 +649,9 @@ public class SettingsFragment extends Fragment {
         resetButton.setVisibility(View.VISIBLE);
         resetButton.setOnClickListener((v) -> {
             debugChannels.clear();
-            debugChannels.addAll(Arrays.asList(DEFAULT_WINE_DEBUG_CHANNELS.split(",")));
-            loadWineDebugChannels(view, debugChannels);
-        });
+            debugChannels.addAll(Arrays.asList(getDefaultWineDebugChannels(context).split(",")));
+            loadWineDebugChannels(view, debugChannels);
+        });
         container.addView(itemView);
 
         for (int i = 0; i < debugChannels.size(); i++) {
@@ -663,7 +665,32 @@ public class SettingsFragment extends Fragment {
             });
             container.addView(itemView);
         }
-    }
+    }
+
+    public static String getDefaultWineDebugChannels(Context context) {
+        String cached = cachedDefaultWineDebugChannels;
+        if (cached != null && !cached.isEmpty()) return cached;
+
+        String fallback = DEFAULT_WINE_DEBUG_CHANNELS;
+        if (context == null) return fallback;
+
+        try {
+            JSONArray jsonArray = new JSONArray(FileUtils.readString(context, "wine_debug_channels.json"));
+            ArrayList<String> channels = new ArrayList<>();
+            for (int i = 0; i < jsonArray.length(); i++) {
+                String channel = jsonArray.optString(i, "").trim();
+                if (!channel.isEmpty()) channels.add(channel);
+            }
+            if (!channels.isEmpty()) {
+                cachedDefaultWineDebugChannels = String.join(",", channels);
+                return cachedDefaultWineDebugChannels;
+            }
+        }
+        catch (JSONException ignored) {}
+
+        cachedDefaultWineDebugChannels = fallback;
+        return fallback;
+    }
 
     public static void resetEmulatorsVersion(AppCompatActivity activity) {
         SharedPreferences preferences = PreferenceManager.getDefaultSharedPreferences(activity);
diff --git a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
index 453dde6..8b723c1 100644
--- a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
+++ b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
@@ -351,11 +351,11 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
             navigationView.setBackgroundResource(R.color.content_dialog_background_dark);
         }
 
-        boolean enableLogs = preferences.getBoolean("enable_wine_debug", false)
-                || preferences.getBoolean("enable_box64_logs", false)
-                || preferences.getBoolean("enable_fex_logs", false)
-                || preferences.getBoolean("enable_vulkan_api_dump", false)
-                || preferences.getBoolean("enable_turnip_logs", false);
+        boolean enableLogs = preferences.getBoolean("enable_wine_debug", true)
+                || preferences.getBoolean("enable_box64_logs", true)
+                || preferences.getBoolean("enable_fex_logs", true)
+                || preferences.getBoolean("enable_vulkan_api_dump", true)
+                || preferences.getBoolean("enable_turnip_logs", true);
         Menu menu = navigationView.getMenu();
         menu.findItem(R.id.main_menu_logs).setVisible(enableLogs);
         if (XrActivity.isEnabled(this)) menu.findItem(R.id.main_menu_magnifier).setVisible(false);
@@ -498,13 +498,13 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
             LogView.setFilename(getExecutable());
             ProcessHelper.addDebugCallback(debugDialog = new DebugDialog(this));
         }
-        if (preferences.getBoolean("enable_fex_logs", false)) {
+        if (preferences.getBoolean("enable_fex_logs", true)) {
             ProcessHelper.addDebugCallback(new FileDebugLogger(this, "fex"));
         }
-        if (preferences.getBoolean("enable_vulkan_api_dump", false)) {
+        if (preferences.getBoolean("enable_vulkan_api_dump", true)) {
             ProcessHelper.addDebugCallback(new FileDebugLogger(this, "vulkan"));
         }
-        if (preferences.getBoolean("enable_turnip_logs", false)) {
+        if (preferences.getBoolean("enable_turnip_logs", true)) {
             ProcessHelper.addDebugCallback(new FileDebugLogger(this, "turnip"));
         }
 
@@ -1153,8 +1153,8 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
         envVars.put("LC_ALL", lc_all);
         envVars.put("WINEPREFIX", imageFs.wineprefix);
 
-        boolean enableWineDebug = preferences.getBoolean("enable_wine_debug", false);
-        String wineDebugChannels = preferences.getString("wine_debug_channels", SettingsFragment.DEFAULT_WINE_DEBUG_CHANNELS);
+        boolean enableWineDebug = preferences.getBoolean("enable_wine_debug", true);
+        String wineDebugChannels = preferences.getString("wine_debug_channels", SettingsFragment.getDefaultWineDebugChannels(this));
         envVars.put("WINEDEBUG", enableWineDebug && !wineDebugChannels.isEmpty()
                 ? "+" + wineDebugChannels.replace(",", ",+")
                 : "-all"
@@ -1667,7 +1667,7 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
 
         envVars.put("VK_ICD_FILENAMES", imageFs.getShareDir() + "/vulkan/icd.d/wrapper_icd.aarch64.json");
         envVars.put("GALLIUM_DRIVER", "zink");
-        if (preferences.getBoolean("enable_vulkan_api_dump", false)) {
+        if (preferences.getBoolean("enable_vulkan_api_dump", true)) {
             File apiDumpFile = WinlatorLogUtils.createTimestampedLogFile(this, "vulkan_api_dump");
             String currentLayers = envVars.get("VK_INSTANCE_LAYERS");
             String apiLayer = "VK_LAYER_LUNARG_api_dump";
@@ -1712,7 +1712,7 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
 
         envVars.put("WINLATOR_DRIVER_SELECTED", driverProbe.getSelectedDriverId());
         envVars.put("WINLATOR_DRIVER_PROBE_STATUS", driverProbe.isUsable() ? "usable" : "fallback");
-        if (preferences.getBoolean("enable_turnip_logs", false)) {
+        if (preferences.getBoolean("enable_turnip_logs", true)) {
             File mesaLogFile = WinlatorLogUtils.createTimestampedLogFile(this, "turnip_mesa");
             envVars.put("MESA_LOG_FILE", mesaLogFile.getAbsolutePath());
             if (envVars.get("MESA_DEBUG").isEmpty()) {
diff --git a/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java b/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
index dbffbc7..41cdcbb 100644
--- a/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
+++ b/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
@@ -263,8 +263,8 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
         File rootDir = imageFs.getRootDir();
 
         SharedPreferences preferences = PreferenceManager.getDefaultSharedPreferences(context);
-        boolean enableBox64Logs = preferences.getBoolean("enable_box64_logs", false);
-        boolean enableFexLogs = preferences.getBoolean("enable_fex_logs", false);
+        boolean enableBox64Logs = preferences.getBoolean("enable_box64_logs", true);
+        boolean enableFexLogs = preferences.getBoolean("enable_fex_logs", true);
         boolean openWithAndroidBrowser = preferences.getBoolean("open_with_android_browser", false);
         boolean shareAndroidClipboard = preferences.getBoolean("share_android_clipboard", false);
 
