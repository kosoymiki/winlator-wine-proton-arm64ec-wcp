diff --git a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
index 02da927..0bf54c4 100644
--- a/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
+++ b/app/src/main/java/com/winlator/cmod/XServerDisplayActivity.java
@@ -1903,17 +1903,48 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
             }
         }
 
-        String vulkanVersion = graphicsDriverConfig.get("vulkanVersion");
-        if (vulkanVersion == null || vulkanVersion.isEmpty()) {
-            vulkanVersion = "1.3";
+        String requestedVulkanVersion = graphicsDriverConfig.get("vulkanVersion");
+        if (requestedVulkanVersion == null || requestedVulkanVersion.isEmpty()) {
+            requestedVulkanVersion = "auto";
+        }
+        String vulkanPolicy = graphicsDriverConfig.get("vulkanPolicy");
+        if (vulkanPolicy == null || vulkanPolicy.isEmpty()) {
+            vulkanPolicy = "auto".equalsIgnoreCase(requestedVulkanVersion) ? "force_latest" : "manual";
         }
         String versionProbeDriverId = useSystemVulkan ? "System" : driverProbe.getSelectedDriverId();
         String detectedVkVersion = GPUInformation.getVulkanVersion(versionProbeDriverId, this);
         int[] detectedVkParts = parseSemverLoose(detectedVkVersion);
-        String vulkanVersionPatch = String.valueOf(detectedVkParts[2]);
-        vulkanVersion = vulkanVersion + "." + vulkanVersionPatch;
-        envVars.put("WRAPPER_VK_VERSION", vulkanVersion);
+        String effectiveVkVersion;
+        String vkNegotiation;
+        if (detectedVkParts[0] <= 0 || detectedVkParts[1] <= 0) {
+            int[] requestedVkParts = parseSemverLoose(requestedVulkanVersion);
+            if ("auto".equalsIgnoreCase(requestedVulkanVersion) || requestedVkParts[0] <= 0 || requestedVkParts[1] <= 0) {
+                effectiveVkVersion = "1.3.0";
+                vkNegotiation = "detected_unavailable_default_1_3";
+            } else {
+                effectiveVkVersion = requestedVkParts[0] + "." + requestedVkParts[1] + ".0";
+                vkNegotiation = "detected_unavailable_requested";
+            }
+        } else if ("force_latest".equalsIgnoreCase(vulkanPolicy) || "auto".equalsIgnoreCase(requestedVulkanVersion)) {
+            effectiveVkVersion = detectedVkParts[0] + "." + detectedVkParts[1] + "." + detectedVkParts[2];
+            vkNegotiation = "force_latest_detected";
+        } else {
+            int[] requestedVkParts = parseSemverLoose(requestedVulkanVersion);
+            if (requestedVkParts[0] <= 0 || requestedVkParts[1] <= 0) {
+                requestedVkParts = new int[]{detectedVkParts[0], detectedVkParts[1], 0};
+            }
+            int[] effectiveBase = minSemverMajorMinor(requestedVkParts, detectedVkParts);
+            effectiveVkVersion = effectiveBase[0] + "." + effectiveBase[1] + "." + detectedVkParts[2];
+            vkNegotiation = (effectiveBase[0] == requestedVkParts[0] && effectiveBase[1] == requestedVkParts[1])
+                    ? "requested_clamped_patch"
+                    : "requested_downgraded";
+        }
+        envVars.put("WRAPPER_VK_VERSION", effectiveVkVersion);
         envVars.put("WINLATOR_DETECTED_VK_VERSION", detectedVkVersion == null ? "" : detectedVkVersion);
+        envVars.put("WINLATOR_VK_REQUESTED", requestedVulkanVersion);
+        envVars.put("WINLATOR_VK_POLICY", vulkanPolicy);
+        envVars.put("WINLATOR_VK_EFFECTIVE", effectiveVkVersion);
+        envVars.put("WINLATOR_VK_NEGOTIATION", vkNegotiation);
         GraphicsSuitability graphicsSuitability = GraphicsSuitability.evaluate(this, versionProbeDriverId, requestedDriverProbe);
         envVars.put("WINLATOR_GRAPHICS_SUITABILITY_TIER", graphicsSuitability.getTier());
         envVars.put("WINLATOR_GRAPHICS_DRIVER_FAMILY", graphicsSuitability.getDriverFamily());
@@ -3172,9 +3203,15 @@ public class XServerDisplayActivity extends AppCompatActivity implements Navigat
 
         int major = safeParseInt(g1);
         int minor = safeParseInt(g2);
-        int patch = safeParseInt(g3);
-        return new int[]{major, minor, patch};
-    }
+        int patch = safeParseInt(g3);
+        return new int[]{major, minor, patch};
+    }
+
+    private static int[] minSemverMajorMinor(int[] lhs, int[] rhs) {
+        if (lhs[0] < rhs[0]) return new int[]{lhs[0], lhs[1], 0};
+        if (lhs[0] > rhs[0]) return new int[]{rhs[0], rhs[1], 0};
+        return new int[]{lhs[0], Math.min(lhs[1], rhs[1]), 0};
+    }
 
     private static int safeParseInt(String s) {
         if (s == null || s.isEmpty()) return 0;
diff --git a/app/src/main/java/com/winlator/cmod/container/Container.java b/app/src/main/java/com/winlator/cmod/container/Container.java
index 358897d..0d81216 100644
--- a/app/src/main/java/com/winlator/cmod/container/Container.java
+++ b/app/src/main/java/com/winlator/cmod/container/Container.java
@@ -37,8 +37,8 @@ public class Container {
     public static final String DEFAULT_EMULATOR = "FEXCore";
     public static final String DEFAULT_DXWRAPPER = "dxvk+vkd3d";
     public static final String DEFAULT_DXWRAPPERCONFIG = "version=" + DefaultVersion.DXVK + ",framerate=0,async=0,asyncCache=0" + ",vkd3dVersion=" + DefaultVersion.VKD3D + ",vkd3dLevel=12_1" + ",ddrawrapper=" + Container.DEFAULT_DDRAWRAPPER + ",csmt=3" + ",gpuName=NVIDIA GeForce GTX 480" + ",videoMemorySize=2048" + ",strict_shader_math=1" + ",OffscreenRenderingMode=fbo" + ",renderer=gl";
-    public static final String DEFAULT_GRAPHICSDRIVERCONFIG =
-            "vulkanVersion=1.3" + ";version=" + ";blacklistedExtensions=" + ";maxDeviceMemory=0" + ";presentMode=mailbox" + ";syncFrame=0" + ";disablePresentWait=0" + ";resourceType=auto" + ";bcnEmulation=auto" + ";bcnEmulationType=compute" + ";bcnEmulationCache=0" + ";gpuName=Device";
+    public static final String DEFAULT_GRAPHICSDRIVERCONFIG =
+            "vulkanVersion=auto" + ";vulkanPolicy=force_latest" + ";version=" + ";blacklistedExtensions=" + ";maxDeviceMemory=0" + ";presentMode=mailbox" + ";syncFrame=0" + ";disablePresentWait=0" + ";resourceType=auto" + ";bcnEmulation=auto" + ";bcnEmulationType=compute" + ";bcnEmulationCache=0" + ";gpuName=Device";
     public static final String DEFAULT_DDRAWRAPPER = "none";
     public static final String DEFAULT_WINCOMPONENTS = "direct3d=1,directsound=0,directmusic=0,directshow=0,directplay=0,xaudio=0,vcrun2010=1";
     public static final String FALLBACK_WINCOMPONENTS = "direct3d=1,directsound=1,directmusic=1,directshow=1,directplay=1,xaudio=1,vcrun2010=1";
diff --git a/app/src/main/java/com/winlator/cmod/contentdialog/GraphicsDriverConfigDialog.java b/app/src/main/java/com/winlator/cmod/contentdialog/GraphicsDriverConfigDialog.java
index d7a3b21..09c0936 100644
--- a/app/src/main/java/com/winlator/cmod/contentdialog/GraphicsDriverConfigDialog.java
+++ b/app/src/main/java/com/winlator/cmod/contentdialog/GraphicsDriverConfigDialog.java
@@ -117,11 +117,12 @@ public class GraphicsDriverConfigDialog extends ContentDialog {
         return config.get("blacklistedExtensions");
     }
 
-    public static String writeGraphicsDriverConfig() {
-        String graphicsDriverConfig = "vulkanVersion=" + selectedVulkanVersion + ";" +
-                "version=" + selectedVersion + ";" +
-                "blacklistedExtensions=" + blacklistedExtensions + ";" +
-                "maxDeviceMemory=" + StringUtils.parseNumber(selectedDeviceMemory) + ";" +
+    public static String writeGraphicsDriverConfig() {
+        String graphicsDriverConfig = "vulkanVersion=" + selectedVulkanVersion + ";" +
+                "vulkanPolicy=force_latest" + ";" +
+                "version=" + selectedVersion + ";" +
+                "blacklistedExtensions=" + blacklistedExtensions + ";" +
+                "maxDeviceMemory=" + StringUtils.parseNumber(selectedDeviceMemory) + ";" +
                 "presentMode=" + selectedPresentMode + ";" +
                 "syncFrame=" + isSyncFrame + ";" +
                 "disablePresentWait=" + isDisablePresentWait + ";" +
@@ -162,10 +163,13 @@ public class GraphicsDriverConfigDialog extends ContentDialog {
         cbSyncFrame = findViewById(R.id.CBSyncFrame);
         cbDisablePresentWait = findViewById(R.id.CBDisablePresentWait);
 
-        HashMap<String, String> config = parseGraphicsDriverConfig(graphicsDriverConfig);
-
-        String vulkanVersion = config.get("vulkanVersion");
-        String initialVersion = config.get("version");
+        HashMap<String, String> config = parseGraphicsDriverConfig(graphicsDriverConfig);
+
+        String vulkanVersion = config.get("vulkanVersion");
+        if (vulkanVersion == null || vulkanVersion.isEmpty()) {
+            vulkanVersion = "auto";
+        }
+        String initialVersion = config.get("version");
         String blExtensions = config.get("blacklistedExtensions");
         String gpuName = config.get("gpuName");
         String maxDeviceMemory = config.get("maxDeviceMemory");
diff --git a/app/src/main/res/values/arrays.xml b/app/src/main/res/values/arrays.xml
index 4b1262c..001c470 100644
--- a/app/src/main/res/values/arrays.xml
+++ b/app/src/main/res/values/arrays.xml
@@ -80,11 +80,13 @@
         <item>ahb</item>
         <item>opaque</item>
     </string-array>
-    <string-array name="vulkan_version_entries">
-        <item>1.1</item>
-        <item>1.2</item>
-        <item>1.3</item>
-    </string-array>
+    <string-array name="vulkan_version_entries">
+        <item>auto</item>
+        <item>1.1</item>
+        <item>1.2</item>
+        <item>1.3</item>
+        <item>1.4</item>
+    </string-array>
     <string-array name="bcn_emulation_entries">
         <item>none</item>
         <item>partial</item>
