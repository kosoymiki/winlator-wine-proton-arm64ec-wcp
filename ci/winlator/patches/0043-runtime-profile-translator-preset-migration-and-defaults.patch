diff --git a/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java b/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java
index c5c599c..4a1f575 100644
--- a/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java
+++ b/app/src/main/java/com/winlator/cmod/ContainerDetailFragment.java
@@ -434,21 +434,27 @@ public class ContainerDetailFragment extends Fragment {
         sStartupSelection.setSelection(previousStartupSelection != -1 ? previousStartupSelection : Container.STARTUP_SELECTION_ESSENTIAL);
 
         final Spinner sBox64Preset = view.findViewById(R.id.SBox64Preset);
-        Box64PresetManager.loadSpinner("box64", sBox64Preset, isEditMode() ? container.getBox64Preset() : preferences.getString("box64_preset", Box64Preset.COMPATIBILITY));
+        Box64PresetManager.loadSpinner("box64", sBox64Preset, isEditMode() ? container.getBox64Preset() : preferences.getString("box64_preset", RuntimeProfileManager.DEFAULT_BOX64_PRESET));
 
         final Spinner sFEXCoreVersion = view.findViewById(R.id.SFEXCoreVersion);
         boolean hasInstalledFexcore = FEXCoreManager.loadFEXCoreVersion(context, contentsManager, sFEXCoreVersion, isEditMode() ? container.getFEXCoreVersion() : DefaultVersion.FEXCORE);
         setRuntimeHintVisible(view, R.id.TVFEXRuntimeHint, !hasInstalledFexcore);
 
         final Spinner sFEXCorePreset = view.findViewById(R.id.SFEXCorePreset);
-        FEXCorePresetManager.loadSpinner(sFEXCorePreset, isEditMode() ? container.getFEXCorePreset() : preferences.getString("fexcore_preset", FEXCorePreset.INTERMEDIATE));
+        FEXCorePresetManager.loadSpinner(sFEXCorePreset, isEditMode() ? container.getFEXCorePreset() : preferences.getString("fexcore_preset", RuntimeProfileManager.DEFAULT_FEXCORE_PRESET));
 
         final Spinner sRuntimeProfile = view.findViewById(R.id.SRuntimeProfile);
         RuntimeProfileManager.loadSpinner(sRuntimeProfile, isEditMode() ? container.getRuntimeProfile() : preferences.getString("runtime_profile", RuntimeProfile.AUTO));
 
+        String configuredRuntimeProfile = preferences.getString("runtime_profile", RuntimeProfile.AUTO);
         String configuredBox64Preset = preferences.getString("box64_preset", "");
         if (!isEditMode() && (configuredBox64Preset == null || configuredBox64Preset.trim().isEmpty())) {
-            Box64PresetManager.loadSpinner(sBox64Preset, Box64Preset.XAW64_BASELINE);
+            Box64PresetManager.loadSpinner(sBox64Preset, RuntimeProfileManager.getRecommendedBox64PresetId(configuredRuntimeProfile));
+        }
+
+        String configuredFexPreset = preferences.getString("fexcore_preset", "");
+        if (!isEditMode() && (configuredFexPreset == null || configuredFexPreset.trim().isEmpty())) {
+            FEXCorePresetManager.loadSpinner(sFEXCorePreset, RuntimeProfileManager.getRecommendedFexcorePresetId(configuredRuntimeProfile));
         }
         setupUpscaleConfigSection(view);
 
diff --git a/app/src/main/java/com/winlator/cmod/SettingsFragment.java b/app/src/main/java/com/winlator/cmod/SettingsFragment.java
index 704c8c7..99b180f 100644
--- a/app/src/main/java/com/winlator/cmod/SettingsFragment.java
+++ b/app/src/main/java/com/winlator/cmod/SettingsFragment.java
@@ -496,7 +496,7 @@ public class SettingsFragment extends Fragment {
         final Context context = getContext();
 
         Callback<String> updateSpinner = (prefix) -> {
-            Box64PresetManager.loadSpinner(prefix, spinners.get(prefix), preferences.getString(prefix+"_preset", Box64Preset.COMPATIBILITY));
+            Box64PresetManager.loadSpinner(prefix, spinners.get(prefix), preferences.getString(prefix+"_preset", RuntimeProfileManager.DEFAULT_BOX64_PRESET));
         };
 
         Callback<String> onAddPreset = (prefix) -> {
@@ -559,7 +559,7 @@ public class SettingsFragment extends Fragment {
         final Context context = getContext();
 
         Callback<String> updateSpinner = (String prefix) -> {
-            FEXCorePresetManager.loadSpinner(sFEXCorePreset, preferences.getString(prefix + "_preset", FEXCorePreset.COMPATIBILITY));
+            FEXCorePresetManager.loadSpinner(sFEXCorePreset, preferences.getString(prefix + "_preset", RuntimeProfileManager.DEFAULT_FEXCORE_PRESET));
         };
 
         Callback<String> onAddPreset = (String prefix) -> {
@@ -789,7 +789,7 @@ public class SettingsFragment extends Fragment {
                             Spinner sBox64Preset = getView().findViewById(R.id.SBox64Preset);
                             InputStream is = getActivity().getContentResolver().openInputStream(uri);
                             Box64PresetManager.importPreset("box64", getContext(), is);
-                            Box64PresetManager.loadSpinner("box64", sBox64Preset, preferences.getString("box64_preset", Box64Preset.COMPATIBILITY));
+                            Box64PresetManager.loadSpinner("box64", sBox64Preset, preferences.getString("box64_preset", RuntimeProfileManager.DEFAULT_BOX64_PRESET));
                         } catch (FileNotFoundException e) {
                         }
                         break;
@@ -798,7 +798,7 @@ public class SettingsFragment extends Fragment {
                             Spinner sFEXCorePreset = getView().findViewById(R.id.SFEXCorePreset);
                             InputStream is = getActivity().getContentResolver().openInputStream(uri);
                             FEXCorePresetManager.importPreset( getContext(), is);
-                            FEXCorePresetManager.loadSpinner(sFEXCorePreset, preferences.getString("fexcore_preset", FEXCorePreset.INTERMEDIATE));
+                            FEXCorePresetManager.loadSpinner(sFEXCorePreset, preferences.getString("fexcore_preset", RuntimeProfileManager.DEFAULT_FEXCORE_PRESET));
                         } catch (FileNotFoundException e) {
                         }
                         break;
diff --git a/app/src/main/java/com/winlator/cmod/container/Container.java b/app/src/main/java/com/winlator/cmod/container/Container.java
index 1bc68d3..eab4702 100644
--- a/app/src/main/java/com/winlator/cmod/container/Container.java
+++ b/app/src/main/java/com/winlator/cmod/container/Container.java
@@ -14,6 +14,7 @@ import com.winlator.cmod.core.KeyValueSet;
 import com.winlator.cmod.core.WineInfo;
 import com.winlator.cmod.core.WineThemeManager;
 import com.winlator.cmod.fexcore.FEXCorePreset;
+import com.winlator.cmod.runtimeprofile.RuntimeProfileManager;
 import com.winlator.cmod.winhandler.WinHandler;
 import com.winlator.cmod.xenvironment.ImageFs;
 
@@ -71,8 +72,8 @@ public class Container {
     private String cpuListWoW64;
     private String desktopTheme = WineThemeManager.DEFAULT_DESKTOP_THEME;
     private String fexcoreVersion;
-    private String fexcorePreset = FEXCorePreset.INTERMEDIATE;
-    private String box64Preset = Box64Preset.COMPATIBILITY;
+    private String fexcorePreset = RuntimeProfileManager.DEFAULT_FEXCORE_PRESET;
+    private String box64Preset = RuntimeProfileManager.DEFAULT_BOX64_PRESET;
     private File rootDir;
     private JSONObject extraData;
     private String midiSoundFont = "";
diff --git a/app/src/main/java/com/winlator/cmod/container/ContainerNormalizer.java b/app/src/main/java/com/winlator/cmod/container/ContainerNormalizer.java
index cb9b80f..894c821 100644
--- a/app/src/main/java/com/winlator/cmod/container/ContainerNormalizer.java
+++ b/app/src/main/java/com/winlator/cmod/container/ContainerNormalizer.java
@@ -6,6 +6,7 @@ import com.winlator.cmod.core.EnvVars;
 import com.winlator.cmod.core.WineInfo;
 import com.winlator.cmod.fexcore.FEXCorePreset;
 import com.winlator.cmod.runtimeprofile.RuntimeProfile;
+import com.winlator.cmod.runtimeprofile.RuntimeProfileManager;
 
 import org.json.JSONArray;
 import org.json.JSONException;
@@ -43,8 +44,8 @@ public final class ContainerNormalizer {
         result.dxwrapper = fallback(result.rawDxwrapper, Container.DEFAULT_DXWRAPPER);
         result.dxwrapperConfig = fallback(result.rawDxwrapperConfig, Container.DEFAULT_DXWRAPPERCONFIG);
         result.envVars = normalizeEnvVarsString(result.rawEnvVars);
-        result.box64Preset = fallback(result.rawBox64Preset, Box64Preset.COMPATIBILITY);
-        result.fexcorePreset = fallback(result.rawFexcorePreset, FEXCorePreset.INTERMEDIATE);
+        result.box64Preset = fallback(result.rawBox64Preset, RuntimeProfileManager.DEFAULT_BOX64_PRESET);
+        result.fexcorePreset = fallback(result.rawFexcorePreset, RuntimeProfileManager.DEFAULT_FEXCORE_PRESET);
         result.box64Version = ContentsManager.normalizeRuntimeVersionToken(result.rawBox64Version);
         result.fexcoreVersion = ContentsManager.normalizeRuntimeVersionToken(result.rawFexcoreVersion);
         result.lcAll = trimToEmpty(result.rawLcAll);
diff --git a/app/src/main/java/com/winlator/cmod/runtimeprofile/RuntimeProfileManager.java b/app/src/main/java/com/winlator/cmod/runtimeprofile/RuntimeProfileManager.java
index ee0ce6a..a0b018d 100644
--- a/app/src/main/java/com/winlator/cmod/runtimeprofile/RuntimeProfileManager.java
+++ b/app/src/main/java/com/winlator/cmod/runtimeprofile/RuntimeProfileManager.java
@@ -5,11 +5,16 @@ import android.widget.ArrayAdapter;
 import android.widget.Spinner;
 import android.widget.SpinnerAdapter;
 
+import com.winlator.cmod.box64.Box64Preset;
 import com.winlator.cmod.core.EnvVars;
+import com.winlator.cmod.fexcore.FEXCorePreset;
 
 import java.util.ArrayList;
 
 public final class RuntimeProfileManager {
+    public static final String DEFAULT_BOX64_PRESET = Box64Preset.XAW64_BASELINE;
+    public static final String DEFAULT_FEXCORE_PRESET = FEXCorePreset.DEVICE_MID_2026;
+
     private RuntimeProfileManager() {}
 
     public static ArrayList<RuntimeProfile> getProfiles(Context context) {
@@ -24,8 +29,78 @@ public final class RuntimeProfileManager {
         return profiles;
     }
 
+    public static String normalizeId(String profileId) {
+        String id = profileId != null ? profileId.trim() : "";
+        if (id.equals(RuntimeProfile.LEGACY_LOW_2026)
+                || id.equals(RuntimeProfile.MID_2026)
+                || id.equals(RuntimeProfile.UPPER_MID_2026)
+                || id.equals(RuntimeProfile.FLAGSHIP_2026)
+                || id.equals(RuntimeProfile.S8G1_BALANCED)
+                || id.equals(RuntimeProfile.S8G1_SUPER)
+                || id.equals(RuntimeProfile.AUTO)) {
+            return id;
+        }
+        return RuntimeProfile.AUTO;
+    }
+
+    public static String getRecommendedBox64PresetId(String runtimeProfileId) {
+        String id = normalizeId(runtimeProfileId);
+        return switch (id) {
+            case RuntimeProfile.LEGACY_LOW_2026 -> Box64Preset.DEVICE_LEGACY_LOW_2026;
+            case RuntimeProfile.MID_2026 -> Box64Preset.DEVICE_MID_2026;
+            case RuntimeProfile.UPPER_MID_2026 -> Box64Preset.DEVICE_UPPER_MID_2026;
+            case RuntimeProfile.FLAGSHIP_2026 -> Box64Preset.DEVICE_FLAGSHIP_2026;
+            case RuntimeProfile.S8G1_BALANCED -> Box64Preset.DEVICE_UPPER_MID_2026;
+            case RuntimeProfile.S8G1_SUPER -> Box64Preset.DEVICE_S8G1_SUPER;
+            default -> DEFAULT_BOX64_PRESET;
+        };
+    }
+
+    public static String getRecommendedFexcorePresetId(String runtimeProfileId) {
+        String id = normalizeId(runtimeProfileId);
+        return switch (id) {
+            case RuntimeProfile.LEGACY_LOW_2026 -> FEXCorePreset.DEVICE_LEGACY_LOW_2026;
+            case RuntimeProfile.MID_2026 -> FEXCorePreset.DEVICE_MID_2026;
+            case RuntimeProfile.UPPER_MID_2026 -> FEXCorePreset.DEVICE_UPPER_MID_2026;
+            case RuntimeProfile.FLAGSHIP_2026 -> FEXCorePreset.DEVICE_FLAGSHIP_2026;
+            case RuntimeProfile.S8G1_BALANCED -> FEXCorePreset.DEVICE_UPPER_MID_2026;
+            case RuntimeProfile.S8G1_SUPER -> FEXCorePreset.DEVICE_S8G1_SUPER;
+            default -> DEFAULT_FEXCORE_PRESET;
+        };
+    }
+
+    public static String resolveBox64PresetId(String runtimeProfileId, String requestedPresetId) {
+        String requested = requestedPresetId != null ? requestedPresetId.trim() : "";
+        String normalizedProfile = normalizeId(runtimeProfileId);
+        if (!RuntimeProfile.AUTO.equals(normalizedProfile) && isLegacyBox64Preset(requested)) {
+            return getRecommendedBox64PresetId(normalizedProfile);
+        }
+        return requested.isEmpty() ? DEFAULT_BOX64_PRESET : requested;
+    }
+
+    public static String resolveFexcorePresetId(String runtimeProfileId, String requestedPresetId) {
+        String requested = requestedPresetId != null ? requestedPresetId.trim() : "";
+        String normalizedProfile = normalizeId(runtimeProfileId);
+        if (!RuntimeProfile.AUTO.equals(normalizedProfile) && isLegacyFexPreset(requested)) {
+            return getRecommendedFexcorePresetId(normalizedProfile);
+        }
+        return requested.isEmpty() ? DEFAULT_FEXCORE_PRESET : requested;
+    }
+
+    private static boolean isLegacyBox64Preset(String presetId) {
+        return presetId.isEmpty()
+                || Box64Preset.COMPATIBILITY.equals(presetId)
+                || Box64Preset.INTERMEDIATE.equals(presetId);
+    }
+
+    private static boolean isLegacyFexPreset(String presetId) {
+        return presetId.isEmpty()
+                || FEXCorePreset.COMPATIBILITY.equals(presetId)
+                || FEXCorePreset.INTERMEDIATE.equals(presetId);
+    }
+
     public static EnvVars getEnvVars(Context context, String profileId) {
-        String id = profileId != null && !profileId.trim().isEmpty() ? profileId.trim() : RuntimeProfile.AUTO;
+        String id = normalizeId(profileId);
         EnvVars envVars = new EnvVars();
 
         // Baseline shared knobs (independent from FEX/Box profile overlays).
@@ -94,9 +169,10 @@ public final class RuntimeProfileManager {
     public static void loadSpinner(Spinner spinner, String selectedId) {
         Context context = spinner.getContext();
         ArrayList<RuntimeProfile> profiles = getProfiles(context);
+        String normalizedSelected = normalizeId(selectedId);
         int selectedPosition = 0;
         for (int i = 0; i < profiles.size(); i++) {
-            if (profiles.get(i).id.equals(selectedId)) {
+            if (profiles.get(i).id.equals(normalizedSelected)) {
                 selectedPosition = i;
                 break;
             }
diff --git a/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java b/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
index 05fe12c..c578335 100644
--- a/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
+++ b/app/src/main/java/com/winlator/cmod/xenvironment/components/GuestProgramLauncherComponent.java
@@ -53,8 +53,8 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
     private String[] bindingPaths;
     private EnvVars envVars;
     private WineInfo wineInfo;
-    private String box64Preset = Box64Preset.COMPATIBILITY;
-    private String fexcorePreset = FEXCorePreset.INTERMEDIATE;
+    private String box64Preset = RuntimeProfileManager.DEFAULT_BOX64_PRESET;
+    private String fexcorePreset = RuntimeProfileManager.DEFAULT_FEXCORE_PRESET;
     private Callback<Integer> terminationCallback;
     private static final Object lock = new Object();
     private final ContentsManager contentsManager;
@@ -291,16 +291,20 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
 
         EnvVars envVars = new EnvVars();
 
-        addBox64EnvVars(envVars, enableBox64Logs);
-        envVars.putAll(FEXCorePresetManager.getEnvVars(context, fexcorePreset));
-
         String runtimeProfile = preferences.getString("runtime_profile", RuntimeProfile.AUTO);
         if (container != null && !isEmpty(container.getRuntimeProfile()))
             runtimeProfile = container.getRuntimeProfile();
         if (shortcut != null)
             runtimeProfile = shortcut.getExtra("runtimeProfile", runtimeProfile);
-        if (isEmpty(runtimeProfile))
-            runtimeProfile = RuntimeProfile.AUTO;
+        runtimeProfile = RuntimeProfileManager.normalizeId(runtimeProfile);
+
+        String requestedBox64Preset = box64Preset;
+        String requestedFexcorePreset = fexcorePreset;
+        String effectiveBox64Preset = RuntimeProfileManager.resolveBox64PresetId(runtimeProfile, requestedBox64Preset);
+        String effectiveFexcorePreset = RuntimeProfileManager.resolveFexcorePresetId(runtimeProfile, requestedFexcorePreset);
+
+        addBox64EnvVars(envVars, enableBox64Logs, effectiveBox64Preset);
+        envVars.putAll(FEXCorePresetManager.getEnvVars(context, effectiveFexcorePreset));
         envVars.putAll(RuntimeProfileManager.getEnvVars(context, runtimeProfile));
 
         if (forensicMode || !isEmpty(forensicTraceId)) {
@@ -309,7 +313,11 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
                     ForensicLogger.fields(
                             "container_id", container != null ? container.id : 0,
                             "route", forensicRouteSource,
-                            "runtime_profile", runtimeProfile));
+                            "runtime_profile", runtimeProfile,
+                            "requested_box64_preset", requestedBox64Preset,
+                            "effective_box64_preset", effectiveBox64Preset,
+                            "requested_fexcore_preset", requestedFexcorePreset,
+                            "effective_fexcore_preset", effectiveFexcorePreset));
         }
 
         String renderer = GPUInformation.getRenderer(null, null);
@@ -559,8 +567,8 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
                             "requested_emulator", emulator,
                             "hodll", envVars.get("HODLL"),
                             "runtime_profile", runtimeProfile,
-                            "box64_preset", box64Preset,
-                            "fexcore_preset", fexcorePreset,
+                            "box64_preset", effectiveBox64Preset,
+                            "fexcore_preset", effectiveFexcorePreset,
                             "box64_version", !isEmpty(box64VersionOverride) ? box64VersionOverride : container != null ? container.getBox64Version() : "",
                             "fexcore_version", !isEmpty(fexcoreVersionOverride) ? fexcoreVersionOverride : container != null ? container.getFEXCoreVersion() : "",
                             "wine_runtime_class", wineRuntimeClass,
@@ -714,7 +722,7 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
         return parsed;
     }
 
-    private void addBox64EnvVars(EnvVars envVars, boolean enableLogs) {
+    private void addBox64EnvVars(EnvVars envVars, boolean enableLogs, String presetId) {
         envVars.put("BOX64_NOBANNER", ProcessHelper.PRINT_DEBUG && enableLogs ? "0" : "1");
         envVars.put("BOX64_DYNAREC", "1");
 
@@ -723,7 +731,7 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
             envVars.put("BOX64_DYNAREC_MISSING", "1");
         }
 
-        envVars.putAll(Box64PresetManager.getEnvVars("box64", environment.getContext(), box64Preset));
+        envVars.putAll(Box64PresetManager.getEnvVars("box64", environment.getContext(), presetId));
         envVars.put("BOX64_X11GLX", "1");
         envVars.put("BOX64_NORCFILES", "1");
     }
