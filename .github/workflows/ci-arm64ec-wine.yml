name: Build Wine 11.1 ARM64EC (WCP)

on:
  push:
    branches: ["main"]
    paths:
      - "build.sh"
      - "ci/ci-build.sh"
      - "ci/lib/**"
      - ".github/workflows/ci-arm64ec-wine.yml"
      - "docs/**"
      - "prefixPack.txz"
  workflow_dispatch:
    inputs:
      wine_ref:
        description: "Git ref in AndreRH/wine"
        required: false
        default: "arm64ec"
      llvm_mingw_ver:
        description: "llvm-mingw version tag"
        required: false
        default: "20260210"
      compress:
        description: "Compression: xz or zst"
        required: false
        default: "xz"

concurrency:
  group: wcp-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  WCP_NAME: wine-11.1-arm64ec
  WCP_OUTPUT_DIR: ${{ github.workspace }}/out/wine
  WINE_REF: ${{ github.event.inputs.wine_ref || 'arm64ec' }}
  LLVM_MINGW_VER: ${{ github.event.inputs.llvm_mingw_ver || '20260210' }}
  WCP_COMPRESS: ${{ github.event.inputs.compress || 'xz' }}
  TOOLCHAIN_DIR: ${{ github.workspace }}/.cache/llvm-mingw
  TARGET_HOST: aarch64-linux-gnu
  WCP_ENABLE_SDL2_RUNTIME: "1"

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Bootstrap packages (Ubuntu)
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            bash curl git python3 wget unzip tar pkg-config \
            cmake ninja-build rsync \
            zstd file binutils nasm \
            build-essential \
            libfreetype-dev fonts-dejavu-core \
            libglib2.0-dev gstreamer1.0-tools \
            libxslt1-dev libxml2-dev \
            libpng-dev \
            liblcms2-dev \
            libunwind-dev \
            libgstreamer-plugins-base1.0-dev \
            libgif-dev \
            libgnutls28-dev \
            libsmbclient-dev \
            libsdl2-dev
          mkdir -p "${WCP_OUTPUT_DIR}" "${TOOLCHAIN_DIR}"

      - name: Cache llvm-mingw toolchain
        uses: actions/cache@v4
        with:
          path: ${{ env.TOOLCHAIN_DIR }}
          key: llvm-mingw-${{ env.LLVM_MINGW_VER }}

      - name: Build Wine 11.1 ARM64EC WCP
        run: |
          set -euxo pipefail
          chmod +x build.sh
          ./build.sh

      - name: Verify outputs
        run: |
          set -euxo pipefail
          ls -lah "${WCP_OUTPUT_DIR}"
          test -f "${WCP_OUTPUT_DIR}/${WCP_NAME}.wcp"
          test -f "${WCP_OUTPUT_DIR}/SHA256SUMS"

      - name: Upload WCP artifact
        uses: actions/upload-artifact@v4
        with:
          name: wine-11.1-arm64ec
          path: |
            out/wine/wine-11.1-arm64ec.wcp
            out/wine/SHA256SUMS
            out/wine/logs/**
          if-no-files-found: error
          retention-days: 14

      - name: Prepare release notes
        if: github.event_name != 'pull_request'
        run: |
          set -euxo pipefail
          cat > RELEASE_NOTES.md <<EOF_NOTES
          Wine 11.1 ARM64EC WCP
          Commit: ${{ github.sha }}
          Workflow run: ${{ github.run_id }}
          EOF_NOTES

      - name: Publish to GitHub Release
        if: github.event_name != 'pull_request'
        uses: ncipollo/release-action@v1
        with:
          tag: wcp-latest
          name: WCP Latest Build
          allowUpdates: true
          prerelease: true
          removeArtifacts: false
          artifacts: out/wine/wine-11.1-arm64ec.wcp,out/wine/SHA256SUMS
          artifactErrorsFailBuild: true
          bodyFile: RELEASE_NOTES.md
